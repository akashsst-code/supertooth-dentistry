# Super Tooth Dentistry — Webflow Build Spec

Consolidated, buildable spec synthesizing every locked decision from:
`supertooth-priority-dimensions.md` · `supertooth-build-principles.md` · `supertooth-ux-flow.md` · `supertooth-navigation-requirements.md` · `supertooth-decision-framework.md`

This is the handoff document for the Webflow/Cowork build itself. Treat every "LOCKED" item as buildable now; every "OPEN" item as a blocker to flag, not to guess past.

---

## 1. Project Goal (context for every build choice)

Grow new patients from ~10–15/month to ~52–69/month (~4–5x), primarily by fixing two conversion leaks:
1. Form submissions → email → manual callback (patients drop off waiting)
2. Missed calls → voicemail → patient calls a competitor

Primary persona: **routine/proactive + insurance-driven**, downtown Seattle, time-scarce professional, unified single-page-led homepage journey (not segmented).

---

## 2. Site Architecture

- **Homepage** — carries the full conversion journey (see Section 3). This is where most persuasion work happens.
- **Service pages** (one per service) — full-depth template (description, FAQ as direct Q&A pairs, insurance/pricing note), content filled in incrementally post-launch. AEO/SEO-structured per Build Constraints.
- **About / Meet the Team** — Dr. Archana + staff/hygienists, credentials, real photography (content pending — see Section 8 blockers)
- **Insurance & New Patients** — carriers accepted, new-patient offer detail, what-to-expect for a first visit
- **Contact** — hours, address, map, parking/transit info, phone

Clean, distinct URLs per service/location page (SEO-ready structure, per locked Build Constraints).

---

## 3. Homepage Section-by-Section Spec

1. **Hero** — practice name, "accepting new patients" eyebrow, insurance-acceptance signal (pulled forward, not buried), primary CTA (Book Now → Tab32 service layer), secondary CTA (Call)
2. **Trust block** — Dr. Archana credentials/photo, team/staff section (photo + name + role), office photo slideshow, Google reviews (4.9★, review count), differentiators (same-day, tech/same-day crowns, in-network)
3. **Insurance/offer block** — carriers listed, $149 new-patient offer (exam + cleaning + x-rays), $500 off Invisalign as secondary line. Positioned *after* trust, not before (locked ordering decision).
4. **Booking block** — Tab32-backed real-time booking widget/CTA
5. **Secondary path** — "Need to be seen today?" hours-aware widget (phone-first during office hours, self-serve/text fallback outside hours)
6. **Footer** — hours, location/parking/transit, phone, standard trust closers

**Ordering rationale:** trust-first, insurance-signal-early, offer-as-reinforcement — protects against reading as a discount clinic, matches the locked "blend, not urgency/discount-led" tone.

---

## 4. Navigation Spec

**Desktop:** logo left · Services / About / Insurance & New Patients / Contact · persistent Book Now CTA right-aligned · sticky header.

**Mobile (Pattern A, locked):**
- Header: logo, call icon (44×44px tap target), Book Now button, hamburger (44×44px tap target) — sticky
- Hamburger opens full-screen menu: quick links (Services / About / Insurance / Contact), **hours/location shown inline** (today's hours + optional live "open now" status + address — reuses the same hours-aware logic pattern as the urgent-path widget), Book Now + phone number repeated at the bottom of the menu

**Explicitly ruled out:** persistent bottom bar (Option B) — app-like feel outweighed benefit.

---

## 5. Design System (implement as Webflow Variables/tokens — not hardcoded)

**Color** (reversible default, terracotta-on-ivory):
- Warm Ivory `#FAF8F4` — dominant surface (~90%)
- Terracotta `#C1633E` — CTAs, star ratings, accents only
- Espresso `#3D3226` — headline/structural text
- Sand tint `#EEE7D8` — badges/card backgrounds

**Typography:**
- Display/headlines: Fraunces
- Body: Inter (sans-serif, healthcare-accessibility standard)
- Body text minimum 16px, line-height ≥1.5x

**Spacing:**
- 8px-based token scale: 8 / 16 / 24 / 32 / 48 / 64 / 96px
- All interactive elements minimum 44×44px tap area, minimum 8px between adjacent targets
- Text spacing: line-height ≥1.5x, paragraph spacing ≥2x font size (WCAG 2.2 SC 1.4.12)

---

## 6. Booking Integration — Technical Spec

**Architecture (locked):** Webflow front end → custom service API layer (built by Akash) → Tab32 API for calendar/schedule data. Not a direct Tab32 embed widget.

**Still open (sub-questions, not yet resolved):**
- Hosting for the service layer (serverless function vs. dedicated backend)
- Auth/API key handling for Tab32
- What the service layer returns to Webflow (available slots, confirmation, booking status)
- How instrumentation (booking-widget completions/drop-off tracking) hooks into this layer

---

## 7. Compliance & Accessibility Checklist (build-blocking, not optional)

- [ ] WCAG AA color contrast verified against locked palette
- [ ] All interactive elements ≥44×44px tap target
- [ ] Body text ≥16px, line-height ≥1.5x
- [ ] Testimonials use Google Reviews widget or first-name + last-initial format (not full patient names without written authorization) — HIPAA
- [ ] No unverifiable claims (same-day availability, insurance carriers, pricing must reflect actual current capability)
- [ ] Privacy policy and required disclosures present before go-live
- [ ] Schema markup: LocalBusiness / Dentist / FAQPage
- [ ] FAQ content structured as direct Q&A pairs (AEO-ready)

---

## 8. Content Blockers (must be resolved before full launch, though build can proceed in parallel)

- [ ] **Team/office photography** — not yet shot. Needs: coordinator (Akash vs. Archana's staff), professional vs. high-quality phone photos, timeline. Blocks Trust block completion specifically — stock imagery should NOT be substituted (undermines the trust signal this section exists to create).
- [ ] Real practice content: exact practice name/address/hours, services list, insurance plans accepted, testimonials/reviews source, logo file. (Dr. Archana bio landed — see Status below.)
- [ ] Tab32 service-layer sub-questions (Section 6) need resolution before booking flow can be built end-to-end.

---

## 9. Instrumentation (build in from the start, per locked principle)

- Form submissions (if any fallback form remains)
- Booking-widget completions and drop-off (via the Tab32 service layer)
- Click-to-call taps
- Page-level traffic

---

## 10. Testing & Approval Gates (per build principles)

1. Goals/spec locked ✅ (this document)
2. Design/wireframe approved before full build
3. Content/copy approved before publishing
4. Functional review (booking end-to-end including failure states, forms, click-to-call on real mobile devices) before go-live
5. Final go-live approval — **Akash only**

Staged rollout via Webflow staging/preview before publishing live. Webflow version history as rollback path.

---

## Status: Next.js build (current — see `supertooth-platform-pivot.md`)

**Desktop design pass — PROPOSED, on `feature/desktop-design-audit`, 2026-09-03. Full audit in `docs/supertooth-desktop-design-audit.md`.** The site had been designed mobile-first and never designed at the other end: below 768px it is what was reviewed, above it the same one-column markup simply got wider. Measured on the running site at 320/768/820/1024/1280/1440/1920px. **Nothing below 768px changes** — every edit is an `lg:`/`xl:`/`sm:` addition or an `md:`→`lg:` move.

- **A WCAG 2.2 SC 1.4.10 Reflow failure was found and fixed.** The homepage scrolled horizontally between 768px and ~866px (`scrollWidth` 864 at a 768px viewport): `EditorialHero`'s grid asked for 824px of hard `minmax()` minimums inside a container with `100vw − 80px`. That band is iPad portrait, a 1536px desktop at 200% browser zoom, and a 1280px desktop at 150%. The two-column split moved `md`→`lg` and both tracks are now `minmax(0, …)`.
- **The homepage got a desktop navigation at last.** At 1440px `EditorialNav` rendered a wordmark and a hamburger and nothing else, with Book/Call hidden until 24px of scroll — so the homepage was the one route where **Pattern A in `supertooth-navigation-requirements.md` (already locked: "desktop = logo, primary links, persistent Book Appointment CTA, sticky") was not implemented**, and NN/g's measurement is that hiding nav costs roughly half of discoverability, worse on desktop than mobile. Four primary links + phone number + persistent Book pill from `lg`; hamburger hidden there. **Neither spec needs amending** — the mobile design spec's clean opening governs the phone composition and is untouched below 1024px. One judgement call to review: `Contact` is filtered out of the homepage link row (the Book pill already points there, and the row does not fit with both at 1024px).
- **The page stopped being a phone page on a desktop screen.** Homepage 12,312px → 8,484px (13.7 → 9.4 desktop screens). `/services` 5,262px → 2,302px (5.8 → 2.6). The services card stack alone was 4,849px — 5.4 screens for five cards — and is now 1,771px as a 2-up/3-up grid. Booking 863px → 479px. Content had been sitting at 768px inside a 1,192px section; at 1920px, 57% of the viewport carried nothing.
- **Reading measure brought inside the band.** Worst case was 88 characters (the 13px sub-service run), FAQ answers 77, service card bodies 76 — against SC 1.4.8's 80 ceiling and the 50–75 comfortable band. Now 67 worst case at 1920px with **zero** paragraphs over 75. Caps are expressed in `ch`, not pixels, so they hold their character count under browser zoom and under the display-settings panel's larger-text option.
- **One correction recorded honestly:** an earlier pass widened `/insurance-new-patients`'s container along with the others and stretched every paragraph on a prose page from ~65 to ~91 characters. Reverted. The rule that came out of it — *widen a container only for content that uses the horizontal axis; cap prose measure in `ch` instead* — is written up in §7 of the audit doc.
- **Mobile affordances stopped leaking onto desktop:** `w-full` pills on `/emergency` and `/offers` were rendering as ~800px-wide buttons and are now rows from `sm`; `/contact`'s email and phone inputs were 558px each and are now paired at 269px. `/emergency`'s tier 1 (911/ER) deliberately keeps the full measure while tiers 2 and 3 share a row, so backlog item 7's "first in DOM and visual order, visible without scrolling" criterion still holds.
- **`Nav.tsx`'s desktop row moved `md`→`lg`**, fixing the 768–790px label wrapping its own code comment already documented — the crowding was a property of the whole row, not just the sixth item it had been patched around.
- **Accessibility entry point: deliberately not duplicated here.** The first-party `DisplaySettings` panel (text size / contrast / motion) is being finished on a parallel branch, and that is the right shape — overlay widgets were researched and ruled out (not endorsed by the W3C's WAI; FTC settled against accessiBe in January 2025; their documented failure modes are double-zoom and a floating button covering magnified content). What this branch contributes is the half a panel cannot provide: the layout now survives being zoomed. Still open and noted for whoever lands that panel — `scroll-margin-top` on anchor targets, so a keyboard user doesn't land under the fixed 64px header.

**Editorial homepage variation — LIVES ON THE `v2` BRANCH, NOT `main`, 2026-09-02:** Akash merged [PR #79](https://github.com/akashsst-code/supertooth-dentistry/pull/79) into a `v2` branch rather than `main`, and [PR #80](https://github.com/akashsst-code/supertooth-dentistry/pull/80) followed it there. **`main` and production are unchanged** — production still serves the original photo-overlay hero and Fraunces/Inter throughout. Treat `v2` as the live proposal and `main` as the shipped site until Akash says otherwise.

What `v2` now contains, beyond the initial variation described in the entry below:

- **Proportions retuned to Akash's reference composition.** Measured as a share of the content column rather than eyeballed, since the reference canvas is 2:3 and no phone is. The headline dropped from the spec's `14vw` to `11vw` ("Dental visits," was 75% of the column against the reference's 59%), the support line 21px → 17px, the button 58px → 52px tall. The reference's own button maps to ~35px, below the touch-target floor, so 52px is a deliberate middle rather than a literal copy. The headline break is now explicit markup — a max-width had re-broken it to "Dental visits, made / better." once the type shrank, orphaning the accent word.
- **Pattern A conflict RESOLVED, not just flagged.** `docs/supertooth-navigation-requirements.md` locks logo + call + booking CTA + hamburger in the mobile header; the mobile design spec bans all of them but the wordmark and menu control. Scroll position now decides: transparent bar with wordmark and menu at the top of the page (the one view the spec governs), then past 24px of scroll it gains a surface and fades in the call icon and a Book pill, behaving like Pattern A from there on. Each requirement gets the state it was actually written about. **Neither doc needs amending.**
- **"Page 2" (`EditorialTrustBlock`) carries the same system down the page** — Manrope at 300–500, Sand ground, terracotta as accent only, one Cormorant italic pull quote. The dark differentiator cards are gone. `TrustBlock.tsx`'s own comment records Akash asking for "a nicer feel like page 1" when page 1 *was* a dark panel; page 1 is now the light editorial hero, so that instruction inverted. **This dropped the tap-to-expand affordance** and with it `expandedNote` and the per-row photos — kept in `content.ts`, unused rather than deleted.
- **Page 2's three rows sit on their own Warm Ivory surfaces** (Akash picked this from three options shown). Honest caveat: this is nearer the card treatment Section 8 steers away from ("subtle dividers rather than large dark cards") — chosen with the trade-off shown, not drifted into.
- **A real bug was found and fixed:** 157px of continuous empty ivory between the hero photo and page 2, 57px of it accidental (`flex-1` with a `max-h` reserves the full screen while the cap stops the photo filling it). The hero box is now 92svh, so the photo fills its column exactly and the spare 8% became a deliberate peek of page 2's Sand ground.
- **The editorial system now runs to the bottom of the page.** Testimonials, Services, Location, New-patient offers, FAQ, Booking and the Footer all moved onto the shared type primitives (`src/components/editorial.tsx` — `Eyebrow`/`SectionHeading`/`Accent`/`Body`/`EditorialSection`) and the ivory/sand ground alternation, so no two adjacent sections share a ground and the Fraunces/Manrope seam below page 2 is closed.
- **Section 4 (Dr. Archana) opens on its heading, not beside the photo, 2026-09-03 (Akash's call).** The eyebrow and heading were the only ones on the page sitting inside a column next to their image; they now run full width above the photograph, which is also what the mobile design spec's own orders say (Section 8's section pattern, eyebrow → heading → copy → "one image, list, or action"; Section 7's hero content order, headline before figure) and what the two blocks above it ("Why choose us", "Our office") already did. The photo/copy pairing below it is unchanged.
- **Credentials made denser without being crowded, 2026-09-03 (Akash: "lesser space, but without crowding").** Nothing removed, no type made smaller. The height came out of three things: `globals.css`'s `p { margin-bottom: 2em }` was landing on all three group labels (~84px of dead space, the biggest single cause), rows dropped py-2 → py-1.5, and the three groups now sit side by side from `md` up — which only works because the block also moved out of the bio's right-hand column into the full section width, so a desktop column is ~350px, *wider* than the old single stack. Measured on the homepage: ~440px → 162px desktop, ~443px → 371px mobile, every row still one line. Columns are opt-in (`<CredentialBadges columns />`); `TrustBlock`/`/about` hand the component a ~600px card where three columns would measure 181px and wrap most rows to two lines, so they keep the stack and take only the spacing win (~440px → 372px).
- **Botox credential reworded to Akash's own wording:** "AACE Trained and Certified Botox Provider". **Open question flagged for Akash:** `content.ts` previously recorded the issuing body for this credential as the American Academy of Facial Esthetics (AAFE), which is a different acronym. Rather than publish an expansion that contradicts the title, the entry's tooltip now states the credential without naming an organisation, and the AAFE attribution is preserved in a code comment so it can go straight back once Akash confirms which is correct.

- **One heading system for the whole page, 2026-09-03 (Akash: "all sections should have similar headline styling ... they aren't carried in later ones").** Testimonials, Services, Location, FAQ and Booking each hand-rolled a near-copy of the section opening — the right font and size, but a bare `<h2>` with no eyebrow and no serif accent word, so only sections 2–4 actually read as the system. They now all use the shared `Eyebrow`/`SectionHeading`/`Accent`/`Body` primitives. New eyebrows are wayfinding labels only ("In their words", "Our services", "Find us", "Good to know") and no headline copy changed beyond italicising one existing word as the accent. `/services` keeps its own default heading and is unaffected.
- **The primitives gained an `onDark` tone** for the one section on the espresso ground. Colour swap only — size, weight, tracking and leading are identical. Forced by contrast, not taste: terracotta on espresso measures ~2.3:1 and fails even the 3:1 large-text bar, so the eyebrow goes Warm Ivory and the accent word goes Sand (~9.9:1), carried by the serif italic rather than by hue. **No new colour token** — both are already locked.
- **New-patient offers removed from the homepage; `/offers` created, 2026-09-03 (Akash).** This reverses the earlier locked "offers stay last, right before the booking ask" placement — both calls are his. `NewPatientOffersBlock.tsx` is kept, not deleted. The two confirmed figures ($149, $500 off Invisalign — item 6) move to the new page and live only there, so `/insurance-new-patients`'s deliberate no-dollar-figures rule still holds. Nothing new is claimed on the page: no expiry, no eligibility terms, no "limited time" framing. Reachable from the menu (see the amendment in `supertooth-navigation-requirements.md`) and added to `sitemap.ts`. Two knock-on fixes: removing it left Location and FAQ both on ivory, so FAQ moved to the Sand ground (and its cards dropped a now-invisible `border-sand`).
- **Booking section restyled, photo removed, footer merged into it, 2026-09-03 (Akash).** It now opens on the same eyebrow/heading/accent as everything above it; the office photo is gone (the page already carries the office carousel, the services photography and Dr. Archana's portrait — a fourth interior shot was repetition, and it was the only reason for the two-column grid); and its container moved onto the standard editorial measure, which also fixed a few-pixel left-edge jog against the section above. Privacy/Accessibility/copyright now read as the closing line of that section. **They were not moved into `BookingBlock`'s markup:** `<footer>` is only a `contentinfo` landmark as a direct child of `<body>`, so burying the row in a `<div>` inside `<main>` would have looked identical and silently dropped the landmark. `Footer` gained a `merged` variant that continues the espresso ground with no seam instead. Every other route renders `Footer` unchanged.
- **Item 61's sitewide AA claim restored.** axe-core found 5 `color-contrast` failures on the homepage — 4 of them predating this pass — all the same root cause: `text-espresso/70` measures 4.12:1 on the Sand ground. Moved to `/80` (5.63:1) in the `Body` primitive and the three inline uses. Homepage, `/offers`, `/services` and `/about` now report 0 violations.

- **Second review pass on the same day — flow top to bottom (Akash).** Five corrections, all measured in the browser rather than eyeballed:
  - **Dr. Archana's heading was the one still rendering plain** — no serif accent, no deliberate break. Now "Experienced care. / Personalized *smiles*.", derived from `archana.tagline` in code rather than retyped, so `content.ts` stays the single source.
  - **The gap above "In their words" was genuinely larger than every other boundary** — 124px against 108px, because `EditorialTrustBlock` carried `pb-20`/`md:pb-28` while the other five sections carried `pb-16`. Six components were each holding their own copy of the same container string, which is how that drifted. There is now one exported `shellWide`, and the boundary is 92px on mobile everywhere — inside the spec's 80–96px band (Section 8), which the old 108px exceeded even before counting the colour change doing separation work alongside it.
  - **"Proudly serving Queen Anne & nearby Seattle" was not the wrong size** (measured 33px mobile / 44px desktop, identical to every other heading) — it was wrapping as "Proudly serving Queen / Anne & nearby Seattle", splitting the neighbourhood's name across lines. Fixed with an explicit break at the sentence, like every other two-line heading here.
  - **The booking section is no longer espresso.** Two spec rules point the same way: Section 8's Final CTA is "a light surface with a single headline and one appointment button", with a dark full-width strip permitted only "near the very bottom"; and Section 4's usage ratio reserves dark for "emphasis and action, not the page background", which a 660px panel holding hours, address, a rating line and a legal row had become. It was the last surface left over from the pre-editorial dark hero — the same inversion already recorded for the differentiator cards. Warm Ivory now, which keeps the ivory/sand alternation against the FAQ above and leaves the terracotta Book pill as the section's only strong colour. Revert is one word (`bg-warm-ivory` -> `bg-espresso`); the `onDark` props on the primitives are what flip the type.
  - **One left spine on desktop.** Services and FAQ took a *centred* `md:max-w-3xl` container, so their headings started at x=296 on a 1280px screen while every other section started at x=64. Both now use the full-width shell with a left-aligned `md:max-w-3xl` on their content instead — the pattern the differentiator list already used. Every eyebrow and heading on the page now starts at the same x. Heading-to-content also normalised to 40px across all sections (Services was 48 at `sm`, Testimonials 48).

- **Third review pass, 2026-09-03 (Akash).** Six more corrections, all measured:
  - **The extra space above Services was real and had a cause.** The testimonial cards are top-aligned in a `w-max` flex track, so the track's height is set by the tallest card whichever one is on screen — and the array led with the *shortest*. A 316px card sat in a 474px track, leaving ~158px of dead space beneath it on top of the section's own padding. Reviews are now sorted longest-first (Akash's call), which zeroes it: measured 0px dead space under the leading card, and the boundary is back to the same 92px as every other one. Sorted in the component rather than reordered in `content.ts`, because that array's comment documents it as a point-in-time Google Business Profile pull in the order pulled.
  - **Location heading is two lines at every width — which cost one word.** "Queen Anne & nearby Seattle." needs 400px on one line against 327px of measure at 375px, so "Proudly serving" on line 1 and the rest on line 2 ran to three lines on every phone. "Proudly" went rather than "nearby": it is the only word there carrying no information, and "Queen Anne & Seattle" would read oddly given Queen Anne is in Seattle. Now "Serving Queen Anne / & nearby *Seattle*." **Flag for Akash if "Proudly" should stay** — the alternative is a smaller size for that one heading, which would break the system just unified.
  - **`SectionHeading`'s clamp floor drops 2rem → 1.875rem.** Below a ~353px viewport the `8.5vw` term is already pinned at the floor, so a 32px floor meant no headline ever got smaller however narrow the phone. At 325px the measure is 272px and "Serving Queen Anne" needs 274px at 32px. Nothing changes at 375px and up.
  - **Standing lines removed under the Location and FAQ headings** (Akash: map immediately; start on the questions). Both were preamble in front of content that explains itself — the map says more than "easy to reach whether you're coming from home, work, or school", and the FAQ line's one substantive half (call us) is already a real answer inside the emergency question and a Quick action below. `SectionHeading` gained a `className` passthrough so those sections keep the same 40px heading-to-content step.
  - **Booking Quick actions rebuilt.** All three were in one `flex-wrap` row: Book and Call filled row 1 to within **12px** of the container's right edge — a dead sliver, not a margin — sat at two different heights (48px vs 50px, from a 2px padding difference), and the emergency badge dropped to row 2 ending **151px** short. Now full-width rows on phones, pairing into two columns from `sm`. That split is measured, not stylistic: "Book Appointment" with its icon needs 170px, and a two-column row inside this group gives it 167px at 390px and 159px at 375px, so side-by-side wrapped the label and made both pills 68px tall. The emergency badge takes the full row and is still visibly the third action, not a fourth primary — text-xs against text-sm, 44px against 48px; only its width changed.
  - **The booking section reads as three groups now.** Quick actions and the hours/location block share the same `max-w-md` cap, so they line up on both a left and a right edge instead of being two blocks of different width, and the run-in above them (body → rating → actions) was evened out.

- **Fourth review pass, 2026-09-03 (Akash) — the booking section.**
  - **Google rating removed from this section** (Akash: "move google reviews out, or keep it centered if appropriate — decide based on customer need"). Moved out, not centered. It was the same figure verbatim as the `TestimonialsSection` badge three sections above (4.9, 427), so a reader reaching the Book button had already passed it; and centering is ruled out by this section's own locked rule ("everything here is left-aligned, never centered", Akash's earlier call). Left-aligned, it split the ask from its own actions. **Trade-off stated for the record:** proof no longer sits adjacent to the CTA, which is a real conversion pattern — if it should return, the place is tight under the body line (8px, not 28px) so it reads as part of the same reassurance block rather than its own row.
  - **Quick actions reorganised again** (Akash: "looking odd with 3 buttons wide"). Two full-width pills, then the emergency link back to the **compact badge** the earlier rounds actually settled on (backlog items 46/48) — restraint through scale. Three equal slabs read as three primaries; two pills plus a small tag reads as a hierarchy. Both remaining constraints are measured at this group's width (342px at 390px, 327px at 375px): "Book Appointment" with its icon needs **170px** against 167px in a two-column row, so pairing wrapped its label and made both pills 68px tall; and "Dental emergency" needs **197px**, so it can't share a row with Call (148px) inside 342px either.
  - **Hours expanded to one row per day** (Akash). Derived in `content.ts` as `hoursByDay` from the existing `hours` ranges rather than typed a second time — the ranges stay the single source, so this can't drift from them or from the `OpeningHoursSpecification` in `layout.tsx` that reads the same array, and expanding a stated range into its days claims nothing new. Day left, time right, the same lookup pattern the nav's hours panel already uses; seven "days · time" sentences would have been a wall. The per-row clock icon is gone — Section 9 rules out one glyph beside every text row, and seven would be noise.
  - **Hours + Location compressed into one panel.** They were two separately-gapped groups each with a label, an icon column and loose row gaps; they are now one sand-tinted panel with a hairline between the day rows and the address, which is what lets the spacing come down without rows running together. Closed days sit at `espresso/80`, not `/60` — on that tint `/60` measures **3.46:1** and fails AA for 14px text, `/80` is 5.97:1; open days stay full-strength so the four bookable ones still read stronger.
  - **Legal row compressed too** — two 12px lines were carrying 20px above and 24px below on top of the section's 48px padding over the rule. The rule is the separator; the whitespace was repeating it. 44px tap targets unchanged (item 12).

- **Fifth pass, 2026-09-03 (Akash) — button treatment and the tail.**
  - **The three Quick actions are one system now.** Every previous version had them sharing nothing: three widths, three fills, and one of them uppercase and letter-spaced while the other two were sentence case — that inconsistency, not the count, is what read as unorganised. They now share width, height, radius, type, icon scale and step; only the **fill** changes, so the ladder is weight rather than size: solid terracotta primary, outlined Call, outlined-in-alert emergency. Nothing there is uppercase any more (it was the only uppercase button on the site). Side-by-side pairs stay ruled out by measurement: at this group's width (342px at 390px, 327px at 375px) two columns give 167/159px, "Book Appointment" with its icon needs 170px, and "Dental emergency" needs 166px sentence-case, so neither can pair with Call at 375px.
  - **Worth a look on review:** the emergency control is outlined rather than solid. Items 46/48 record a solid badge as the settled treatment, arrived at after plain de-emphasized *text* was rejected as "hidden". This is not that — it is a peer-sized button with an icon in alert red at 6.3:1 on this ground, so it is ranked rather than hidden. Reverting to solid is one class.
  - **The tail below Location is cut from 151px to 123px.** Measured at 390px from the last line of the address to the bottom of the page: 32px of it was the section's own bottom padding sitting directly above a hairline rule that already separates the legal row, and the rule then added its own padding on top. Section padding is now 16px/24px, the rule's 12px, the footer's bottom 16px. The 44px tap-target height on Privacy/Accessibility stays — that is item 12's requirement, not slack, and it is the single largest remaining piece.
  - **Caught by lint, worth recording:** one of this round's explanatory comments was written as a bare `/* … */` in JSX children rather than `{/* … */}`, which makes React render it as visible text. `react/jsx-no-comment-textnodes` flagged it and it was fixed before pushing — the served HTML has zero occurrences. This is why the lint count is checked against the base branch every round rather than eyeballed.

- **Legal row alignment and rhythm, 2026-09-03 (Akash).** Two faults, both traceable to the 44px tap targets on the links. The copyright line carried `px-2` to visually match the links' padding, which put its text at **x=32** while every other left edge in the section — buttons, hours panel, and the links' own text — sat at x=24; padding removed, so all four now measure 24 (and 64 at 1280px). And the vertical gaps were **26px above "Privacy" and 0px between the two lines**: a 44px box around 17px text carries ~13px of invisible padding per side, which the container's padding was adding to at the top and not at all in between. Compensated to 20px / 15px / 16px. Tap targets still 44px.

**Still open on `v2`:** the hero carousel still runs `heroPhotos`, art-directed to sit behind a dark scrim — its lead studio headshot has a cool lavender backdrop and `front-desk.jpg` is a dark night shot, both of which fight the light editorial frame; `officePhotos` are the daylit interiors the reference actually uses. **No `--color-*` token was changed anywhere in this work** — the locked palette guardrail holds; the only additions are two font tokens.

**Editorial mobile hero — homepage screen 1, shipped to a branch as a VARIATION FOR REVIEW, not merged, 2026-09-02:** Built to `docs/supertooth-mobile-design-spec.md` (a light, editorial mobile homepage concept Akash supplied), on `feature/mobile-editorial-hero-variation`. Scope is deliberately the above-the-fold view only — `EditorialNav.tsx` + `EditorialHero.tsx`, wired into `src/app/page.tsx`. `Nav.tsx`, `Hero.tsx` and `ViewportHero.tsx` are all still in the tree unchanged and every other route still renders the original `Nav`, so reverting is a two-line change to `page.tsx`.

The composition inverts the current hero's central idea: instead of copy overlaid on a photo behind an espresso scrim, text and photograph never overlap — one headline ("Dental visits, made *better*.", the accent word in serif italic), one support line, one CTA, one 4:5 photograph, separated by whitespace rather than scrims or cards. Real practice photography (`office-2.webp`, the daylit treatment room) matches the spec's art direction without new content sourcing.

**Three decisions on this variation that are Akash's, not settled by the spec:**

1. **Type tokens.** The spec's Manrope + Cormorant Garamond Italic are used here, per Akash's explicit choice when asked, rather than the locked Fraunces + Inter. Implemented additively — new `--font-editorial`/`--font-editorial-serif` tokens; `--font-display`/`--font-sans` are untouched and still drive every other page. If this variation is rejected, the fonts leave with it.
2. **Google reviews in the hero.** A deliberate deviation from the spec, which bans ratings from the opening view entirely (its Sections 1 and 8). Akash asked for reviews to stay on screen 1, so the rating renders in the spec's "small metadata" role — one quiet line below the CTA, no badge or logo block.
3. **Header conflict, unresolved and needing a call before merge.** The spec's mobile header is a wordmark plus a menu control only; `docs/supertooth-navigation-requirements.md` Pattern A locks logo + call icon + booking CTA + hamburger all visible in the mobile header. This variation follows the spec and therefore breaks that locked requirement. Mitigated (call and book are both one tap away in the menu sheet, phone as a real `tel:` link) but not resolved.

**Colors were not touched** — the locked palette guardrail holds. Each of the spec's own tokens was mapped onto an existing one instead: canvas → Warm Ivory, ink → Espresso, ink-muted → `espresso/70`, forest (action + accent) → Terracotta/Terracotta-dark. `git diff` on `globals.css` shows no `--color-*` change. Contrast was computed against the real token values: white on `terracotta-dark` is 5.62:1 (plain `terracotta` would be 4.12:1 and fail for the 16px button label, which is why the button is flat `terracotta-dark`, not the site's terracotta gradient); the accent word is `terracotta` at 3.89:1, AA for large text only and safe solely because it renders at 52px+.

Verified in-browser at 390×844, 360×800 and desktop: no horizontal scroll at 360px, headline clamps 52→68px across the range, CTA 58px tall / 204px wide (spec: 56–60 / 198–220), menu control 44×44, ~400px of photograph visible above the fold as the scroll affordance. Production build and `tsc --noEmit` both clean.

**Known follow-up, out of scope here:** the spec's Section 8 warns against dropping straight from this hero into "a dense grid of dark cards," which is exactly what `TrustBlock` still does immediately below it. Everything below the fold is untouched by this variation, so the tonal jump is real and visible — it needs its own decision if screen 1 is adopted.

**Dental anxiety content shipped, item 16, `not-started` → `done`, 2026-09-03:** v1 minimum only — one non-judgmental paragraph (`content.ts` `anxietyNote`, "about 1 in 5 adults feel the same way," no sedation or comfort-option claims, no v2 items). Placed on `/contact` directly above `AppointmentForm`'s optional "Additional details" field rather than the homepage strip this item originally sketched — Akash's call, since the item's own scope already pairs this content with that field. Verified at 375×812: the paragraph renders as one ~120px block, comfortably within a single mobile screen, immediately followed by the field it points readers to.

**LocalBusiness/Dentist schema, robots.ts, sitemap.ts, and item 37's backlog type-scale gap, items 4/37 → `done`, 2026-09-02–03:** `Dentist` JSON-LD added to `RootLayout`, built from `content.ts` so it can't drift from displayed content; `robots.ts`/`sitemap.ts` added (both exclude `/backlog`). Uses a new `content.ts` `siteUrl` (`www.supertoothdentistry.com`) — **not independently reconfirmed with Akash**, flagged for review before launch. Separately, item 37's one known remaining gap (13 hardcoded 11px labels in `BacklogView.tsx`) is closed, moved onto the `text-xs` token.

**Sitewide WCAG AA contrast fixed — item 61, `not-started` → `done`, launch-blocking, 2026-09-02:** Item 61 was opened by item 29's first Global Test Harness pass (see that entry below) — every one of the 8 patient-facing routes failed axe-core's `color-contrast` check, root-caused to a handful of `text-espresso/40–60` opacity variants and `text-terracotta`-on-Warm-Ivory links recurring across `Footer.tsx`, `AppointmentForm.tsx`, `Nav.tsx` and every page's breadcrumb/last-updated line.

Fixed every failing `text-espresso/NN` callsite (opacity 20 through 65, excluding `aria-hidden` decorative separators) by raising it to `text-espresso/70` — the lowest opacity that clears WCAG AA's 4.5:1 against the locked `#faf8f4` Warm Ivory background at this site's actual 14px fine-print size (measured 4.77:1; confirmed this is normal-size text, not eligible for the 3:1 large-text exception). The `tel:` link in `AppointmentForm.tsx` switched from `text-terracotta` (3.86:1, failing) to the already-defined `text-terracotta-dark` token (5.32:1).

Re-running the harness's own `npx @axe-core/cli` command against all 8 routes post-fix (rather than trusting the two root causes named in item 29's evidence) surfaced 3 more real violations the original pass hadn't caught: `text-warm-ivory/50` on the dark `bg-espresso` panel in `BookingBlock.tsx` (3 eyebrow labels, 4.21:1) and in `TestimonialsSection.tsx` (3.51:1 against its blended card background), plus `text-terracotta` on that same dark panel for `BookingBlock.tsx`'s "Visit us" label (3.04:1) — and because full-opacity terracotta on espresso tops out at 3.04:1 regardless of opacity, that one needed a token swap (to `text-warm-ivory/70`, matching its sibling labels) rather than an opacity bump. Final verification: 0 `color-contrast` violations on all 8 routes at 375×812. No `--color-*` base token value touched (`git diff` on `globals.css` is empty) — every fix was which opacity variant or token a given callsite references, per the item's own acceptance criteria and `docs/CLAUDE.md`'s locked-token guardrail.

`npx tsc --noEmit`, `npx next build`, and `npx tsx scripts/check-backlog.ts` all clean (the one pre-existing `check-backlog` finding, item #57's reference count, predates this PR and is out of scope here).

**Insurance, offers, financing and privacy content verified — items 2, 6, 12 all `partial`/`blocked` → `done`, 2026-09-02:** Akash walked the Section 22 verification table in chat. Confirmed accurate as-is: the six named insurance carriers, the $149 new-patient and $500 Invisalign offers, all five additional service-area neighborhoods, the $50 referral credit, and same-day emergency accommodation. Two corrected rather than simply confirmed:

- **Financing** — content.ts called it "interest-free financing." Checked against CareCredit's own published terms (the practice's actual financing partner): standard promotional financing there is *deferred interest* — 0% only if the balance is paid in full within the promo window, otherwise interest is charged retroactively on the full original amount. Reworded in both FAQ answers and the `/insurance-new-patients` financing line to "financing through CareCredit, including promotional plans with no interest if paid in full within the promotional period" — accurate to how CareCredit actually works, not the blanket claim that was there before.
- **Cancellation policy** — corrected from an invented "48 hours' notice" to the real policy, "2 business days' notice."

Removed `<Placeholder>` wrapping now that the underlying values are confirmed: `InsuranceBlock.tsx`'s carrier grid and "plus most major PPO plans" line, `NewPatientOffersBlock.tsx`'s offer text. `grep -rn "<Placeholder" src/` now returns zero live render sites on production routes — the only remaining reference is `ServicesSection.tsx`'s `real ? ... : <Placeholder>` fallback, dead code today since every service is `real: true`, kept for any future non-real entry.

**Privacy policy rewritten and unblocked, item 12:** Akash confirmed the practice does send appointment texts (the open SMS question) and gave direct instruction on sourcing the privacy text itself — "model, look at all competitors in market and find it" — rather than a separate counsel-review round. Rewrote `privacyPolicy` in content.ts informed by a real comparable Seattle practice's page structure (seattledentalstudio.com) plus standard HIPAA Notice-of-Privacy-Practices elements: added sections for information collected, how it's used (with a pointer to the office's own HIPAA NPP for clinical records), a real SMS/text-messaging section (opt-in, frequency, "reply STOP," no third-party marketing sharing), sharing/disclosure, cookies, patient choices, and a changes-to-this-policy note. Flipped `privacyPolicy.draft` to `false` — the draft banner on `/privacy` no longer renders. Akash can still route this to counsel for extra assurance later; per their own direction, that's not a blocker to publishing now.

`npx next build`, `npx eslint`, and `npx tsx scripts/check-backlog.ts` all clean (no new problems).

**Emergency footer link added, then removed again — items 45 and 7, 2026-09-02/03:** Item 39's audit named a real gap: `BookingBlock.tsx`'s `bg-alert` "Dental emergency" Quick Actions pill only renders on `/` and `/contact`, so every other route (about, services, insurance-new-patients, emergency, privacy, accessibility) had no one-tap emergency path besides the hamburger menu — which item 45's own acceptance bar doesn't count as one tap. Rather than reopening header placement (tried and rejected 2026-09-01) or a bottom bar (declined 2026-08-30), the fix was the simplest one: put "Dental emergency" back in `Footer.tsx`, same `bg-alert` + `MedicalCrossIcon` pill BookingBlock already uses. Verified via DOM measurement at 375×812 (`/services`, `/privacy`) and 1280px: link present, resolved to `/emergency`, 44px tap-target height.

Removed again the same review round — Akash asked to cut it ("remove dental emergency from near privacy accessibility area, since its already covered"). Flagged before acting that this reopens the exact gap the footer link had just closed on 6 of 8 routes, and confirmed the removal was meant sitewide, not just the homepage (where the footer link genuinely did duplicate BookingBlock's pill one section above). Akash confirmed sitewide. `Footer.tsx` is back to just Privacy/Accessibility + copyright.

This is a deliberate, informed tradeoff — items 45's acceptance criteria were rewritten to match what was actually approved (one-tap from `/`/`/contact`, hamburger-menu elsewhere) rather than left pointing at a bar the site no longer tries to clear. Item 7's acceptance no longer hard-codes a placement mechanism ("the footer") — it now defers to item 45 as the single source of truth for reachability, so the two items stop flip-flopping in lockstep every time the placement question gets revisited.

`npx next build` and `npx eslint` both clean.

**Appointment-form double-submit race fixed; item 33 marked `done`, 2026-09-02:** the item 32/33 work below merged to `main` as [PR #70](https://github.com/akashsst-code/supertooth-dentistry/pull/70) — the "left `partial`, not `done`" entry below is superseded on the state-preservation point: the merged version doesn't rely on the Next.js router-cache reasoning it describes, it lifts form state into a new `AppointmentFormStateProvider.tsx` (plain React Context mounted in `RootLayout`, in-memory only, no `localStorage`/`sessionStorage` per the item's PHI-related `outOfScope`) so values survive a client-side route change independent of any cache TTL. Verified post-merge: filled the form, navigated `/contact` → `/` → `/contact` via real `next/link` clicks, values intact. Item 32 stays `partial` regardless — the iOS keyboard/zoom-on-focus and real-backgrounding halves are still real-device-only per its own test spec.

Item 33 (loading and empty states) also landed in that same merge but its `status` field was never updated — this PR fixes that to `done`. The empty-state half is a documented no-op (no search/list surface exists on the site to apply it to, per `docs/supertooth-next-features-proposal.md`); everything else — spinner/label swap, `aria-busy` on the form and button, an `sr-only aria-live` region, submit disabled in flight — is in place and verified: a `PerformanceObserver` on `layout-shift` measured `0` across the loading→confirmation transition, and the busy state is observable within 50ms of the click.

Found a real bug while verifying the double-submit guard: `if (submitting) return` reads React state, which is stale within the same synchronous tick — firing two `.click()` calls back-to-back with no `await` between them both read `submitting === false` and both slipped past the guard before React re-rendered the disabled button (confirmed via a `window.setTimeout` spy: 3 synchronous clicks scheduled 3 submissions). This PR replaces the state check with a `useRef` boolean set synchronously before `setSubmitting(true)`, closing the gap regardless of render timing — re-verified the same way, 3 clicks now schedule exactly 1.

`npx eslint` and `npx tsc --noEmit` both clean.

**Global Test Harness run for the first time — item 29, `not-started` → `partial`, item 61 opened, 2026-09-02:** `src/lib/test-harness.ts` (all 22 GTH checks) already existed and every backlog item already referenced its applicable harness ids — that half of item 29 had shipped without the status field ever being flipped. This pass does the actual run: `npx @axe-core/cli` (GTH-1, wcag2a/2aa/21a/21aa) against all 9 shipped routes at 375×812 on the production deployment, a 320px horizontal-overflow check (GTH-13) on all 9 (clean everywhere), a console-clean spot-check (GTH-9) on 2 of 9, and `npx lighthouse --form-factor=mobile --throttling-method=simulate` (GTH-2/19) on `/` and `/contact`.

Two real findings came out of the axe run. First, `OfficeCarousel.tsx`'s duplicate track (rendered twice for the seamless scroll loop) had its second copy's tiles set `aria-hidden` but not removed from tab order — a keyboard user could still Tab into 5 invisible duplicate "View larger" buttons. One-line fix: `tabIndex={-1}` on the hidden copies, verified clean via a re-run against localhost before pushing. Second, every one of the 8 patient-facing routes failed `color-contrast` — the same handful of `text-espresso/40–60` opacity variants and `text-terracotta` (on Warm Ivory) recur across `Footer.tsx`, `AppointmentForm.tsx` and `Nav.tsx`, all measuring 2.2:1–3.86:1 against the required 4.5:1. This is the "WCAG AA contrast" line in `docs/CLAUDE.md`'s compliance non-negotiables, currently unmet in production — not fixed in this PR (logged as new item 61, `launchBlocking: true`, ground `legal`) because the affected files were under active concurrent edit in several other sessions' worktrees at the time of this pass; fixing in place risked a broken merge or clobbering someone else's in-flight work. `/backlog`'s 4560 contrast violations are excluded, same carve-out item 37 already made for that noindex internal tool.

The Lighthouse run also surfaced a discrepancy worth a follow-up rather than an immediate fix: `/contact` scores cleanly (performance 99, LCP 2.1s) but `/` scores performance 88 / LCP 3.5s / Speed Index 4.1s, below GTH-2's stated budget, despite item 38 (performance budget) being marked `done`. Flagged in item 29's evidence rather than reopening item 38 on a single run.

Left `partial`: the remaining checks (HTML validity, keyboard-only task completion, JS-disabled degradation, thumb-zone/safe-area/landscape, 200%-resize, reduced-motion) weren't run route-by-route in this pass — see item 29's evidence for exactly what's covered vs. still open.

`npx eslint` and `npx tsc --noEmit` both clean on the one file changed (`OfficeCarousel.tsx`).

**Design anti-pattern audit — item 39, `not-started` → `done`, 2026-09-02:** Walked all ten of blueprint §17's named failure modes (generic, untrustworthy, overly corporate/cold, too expensive/exclusive, sterile/clinical, juvenile, clinically intimidating, visually noisy, hard to read, aggressive/sales-driven) against every page at 375px, plus the item's four named shell checks (sitewide superlative/urgency regex scan, autoplay/`prefers-reduced-motion` check, contradictory hours/phone/address check). Full ten-verdict record with evidence lives in `backlog.ts`'s item 39 entry rather than duplicated here.

Seven modes pass outright. Three findings, each fixed or logged with an owner per the item's own acceptance bar:
- **Untrustworthy (fail):** 13 live `<Placeholder>` render sites remain (insurance carriers, new-patient offer/financing copy) — already item 2's exact scope, confirmed still open, not re-logged.
- **Generic (partial):** 6 of the homepage's `services`/`offers` image slots are stock Unsplash photography rather than real practice photos — a deliberate 2026-09-01 trade against Akash's own clinical macro/x-ray shots reading as alarming to patients, not an oversight, but stock imagery is this mode's own named cause. Logged as new **item 60** ("Replace stock Unsplash photography with real practice photos"), blocked on Akash supplying or approving real photos for the slots the existing team/office photo pool doesn't already cover.
- **Hard to read (partial):** a DOM-level check on `/insurance-new-patients` found genuine body copy (the page's own intro sentence, two Placeholder financing/PPO lines) computing at 14px, below the locked Inter-body-16px floor — not just breadcrumbs/legal fine print. Logged against item 37 ("Mobile-first type and spacing scale," already `partial`) rather than a blanket sitewide find-and-replace here, since separating real body copy from intentional fine print needs the same care item 37 already applies.

Superlative/urgency acceptance criteria hold sitewide: zero matches for either regex except one superlative inside a genuine attributed Google review (Karthik B., item 13's real-review pull) — a patient's own words, not marketing copy, so it's the antidote's case, not a violation. `TestimonialsSection.tsx`'s continuous auto-scroll (mode 8, visually noisy) is recorded as a reviewed-and-accepted tradeoff, not reopened — it already skips its animation loop under `prefers-reduced-motion` and exposes a WCAG-2.2.2 pause control. No code changes shipped in this pass — every finding needs either a practice decision (Akash) or belongs to an item already doing that specific work (2, 37) rather than a fix cheap enough to bundle here.

`npx tsc --noEmit` and `npx next build` both clean.

**Homepage FAQ brought to full parity with the live practice site, merged to `main` via [PR #69](https://github.com/akashsst-code/supertooth-dentistry/pull/69):** the original FAQ port ([PR #28](https://github.com/akashsst-code/supertooth-dentistry/pull/28), merged) covered 9 of the source site's 13 real FAQ items. Akash asked for the remaining ones brought over too, so `faqs` in `content.ts` now has all 13: added "What is Super Tooth's philosophy?", "Do you offer any new-patient offers?", "What other forms will I need to fill out, and when?", and "What procedures do you offer?" (the 14th item on the source site, its SMS Privacy Policy blurb, stays excluded — a legal disclosure, not an FAQ, same call as the original port).

Same rewrite-and-trim pass as the original 9, not copy-paste: the philosophy answer drops the source's unverifiable "10x lower radiation than conventional x-rays" claim, and the new-patient-offers answer has no dollar amount (that already lives in `offers`, flagged unconfirmed). Akash also flagged the source site's insurance FAQ as the pattern to avoid — an FAQ answer that hands the visitor off to another page/site instead of just answering — so all four new entries are self-contained plain text with no outbound link or image; "What procedures do you offer?" names the Services section rather than linking to it. No changes to `FAQSection.tsx` itself.

A follow-up side-by-side audit against the live site's exact wording (Akash's ask, after flagging the port looked heavily trimmed) found the compliance trims were correct but turned up separate gaps worth a second pass: several answers dropped verifiable procedural detail that isn't a compliance issue (comprehensive-exam and what-to-bring answers, mainly), and two answers changed meaning rather than just length — the sedation answer drops that oral sedation is available in some cases, and the cancellation answer gives a different reason (no fee mentioned) than the source's "to avoid charges." Neither is fixed in this PR; flagged for Akash to confirm current policy before a follow-up commit.

**Mobile input correctness on the appointment form — item 32, status `not-started` → `partial`, PR open, not yet merged, 2026-09-02:** Picked as the next unblocked P0: it's `dependsOn: null`, mechanical (no wording/placement call for Akash to make, unlike items 16/45), and a parallel proposal doc (`docs/supertooth-next-features-proposal.md`) had already flagged it as ready to start without further review.

Audited `AppointmentForm.tsx`'s five fields against the item's three named defects before touching anything: font-size was **already fine** — every input/textarea measures 17px (Tailwind v4's Preflight resets form-control font-size to inherit from `body`, and item 37's type-scale pass already set that to 17px) — so no iOS zoom-on-focus risk exists today, contrary to what the item's `problem` field assumed going in. What was actually missing: `inputmode` was unset on every field. Added the full `type`/`inputmode`/`autocomplete` triplet — `email`→`inputMode="email"`, `phone`→`inputMode="tel"`, and `firstName`/`lastName`→`inputMode="text"` for completeness even though `text` is already the default keyboard. Verified in-browser post-fix: all five fields report the correct triplet, all still at 17px, no horizontal overflow at 375px, submit button confirmed `position: static` (never in a fixed/sticky footer a keyboard could permanently cover).

**Left `partial`, not `done`:** the item's third defect — entered values surviving back-navigation/backgrounding — has no code change here. Checked what's actually true instead of assuming a fix was needed: `/contact` is statically prerendered (confirmed via `next build` output), which per Next.js's own `staleTimes` docs gets a 5-minute client-side cache window for back/forward navigation by default (only *dynamic* segments default to 0s, changed in Next 15) — combined with `Nav.tsx` already using `next/link` everywhere, this should already survive the item's own test scenario (part-fill, navigate away, come back) without needing any storage-based workaround, which the item's own `outOfScope` correctly rules out for PHI reasons. Plain backgrounding (switching apps to find an insurance card) never unmounts React at all — nothing in this component would trigger a reset if it did stay mounted. Not marking this `done`, though: the item's own test spec tags this specific criterion `manual` / real-device-only, and that hasn't been run. Needs a real-phone check before this item can close.

`npx tsc --noEmit` and `npx next build` both clean.

**"Why Choose Us" accordion — accessibility and motion fix, PR [#64](https://github.com/akashsst-code/supertooth-dentistry/pull/64), merged 2026-09-02:** Akash supplied `mobile-pattern-spec.md`, a portable mobile accordion/before-after design spec, and asked for it applied to the homepage's "Why Choose Us" section (`TrustBlock.tsx`, built on the shared `ExpandCard` shell in `InsuranceExpandCard.tsx` that FAQSection and the insurance card also use).

Two parts of that spec were deliberately not built, confirmed with Akash before starting: its before/after photo comparison widget conflicts with the locked backlog decision deferring before/after imagery until a consent workflow exists (no signed patient photo authorization exists yet); its own `--card`/`--accent`/etc. color tokens conflict with this site's locked palette (CLAUDE.md's base-color-token guardrail) — kept the existing Espresso/Terracotta/Warm-Ivory tokens instead.

What did ship, scoped to the parts of the spec that are unambiguous accessibility/motion best practice and didn't touch content, layout or color: fixed a real bug where a closed card's content — including its "Book Appointment" link — stayed reachable by keyboard/screen-reader despite being visually clipped to zero height (`inert={!open}` now removes it from the interaction tree when closed); wired `aria-controls`/`id` between each toggle and its panel per the standard disclosure-widget pattern (previously `aria-expanded` pointed at nothing); retuned the expand/collapse transition to a 240ms-open/180ms-close decelerate curve with a `prefers-reduced-motion` override (was a flat 300ms both ways, no reduced-motion handling); and made an opened card scroll its own header back into view if expansion pushed it off-screen.

Verified on the PR's Vercel preview at 375×812: a closed panel reports `inert: true` (excluded from the accessibility tree), and opening a card flips `aria-expanded="true"`, clears `inert`, and resolves `aria-controls` to the correct panel id.

**BookingBlock/Footer closing-CTA pass — item 34 `partial`, merged to `main` via [PR #63](https://github.com/akashsst-code/supertooth-dentistry/pull/63):** Akash supplied an external footer-redesign spec (mobile-first closing CTA + footer, ten layout options). Most of it duplicates architecture this repo already locked or explicitly ruled out — a sticky mobile Book/Call bar was tried and rejected (item 45's `outOfScope` note), a live "open now/closed" indicator is explicitly out of scope for item 34 (a static hours list is the accepted v1), and `TestimonialsSection.tsx`'s rating display is a deliberately non-link "pure trust display." Scoped this PR to the pieces that are both in the spec and compatible with those calls — started on top of `feature/phone-emergency-legal-reviews` (PR #56) rather than `main` since that branch owned `Footer.tsx`/`content.ts`/`BookingBlock.tsx` at the time; #56 merged first, so this PR merged `main` back in (resolving one conflict in this file's own status log) and now targets `main` directly:

- **Item 34 (`partial`, was `blocked`):** its `dependsOn` (Akash confirming hours match the GBP) was already satisfied by item 3. `BookingBlock.tsx` states the 7:00 AM open inline in the "Visit us" eyebrow ("Visit us · Open from 7:00 AM"), and the hours list below states both rows honestly — the open range and "Saturday – Monday · Closed" — instead of hiding the closed one. Still open: the early-open mention hasn't been added to Nav or `LocationMapSection.tsx`, the item's other two `where` surfaces.
- **Trust proof near the booking decision:** a plain-text Google rating line (stars + `4.9` + `427 Google reviews`, same non-link pattern already used in `Hero.tsx`/`TestimonialsSection.tsx`) now sits under `BookingBlock.tsx`'s reassurance line, since item 13 made that data real.
- **Deliberately not built:** a sticky mobile Book/Call bar, any new emergency phone number or contact route (still none confirmed), and a new "amber" colour token — emergency styling continues to reuse the existing `--color-alert` token, which already serves that exact role.

**First-pass review, revised same day (2026-09-02) per Akash's direct feedback on the live preview:** the initial version of this PR shipped the 7:00 AM open as a separate pill badge next to "Visit us," a "Get Directions" link added to `BookingBlock.tsx`'s Location column and both `Footer.tsx` variants, and left the pre-existing "New patient? Start here" line in place. Akash flagged the badge as "floating" (a disconnected chip rather than one clean line), the hours/location text as wrapping mid-phrase in too narrow a column, and asked to drop both the Get Directions links and the New Patient line. Revised: the open-hour badge is now plain text inside the eyebrow line; Office hours/Location moved from a cramped two-column grid to full-width stacked rows (with a fixed icon-column grid per row so any wrap indents under the text, not the icon) — no more mid-phrase wraps at any tested width; both Get Directions additions are reverted (address is plain text again, matching pre-PR behavior); "New patient? Start here" is removed (`/insurance-new-patients` stays reachable via the persistent nav). Verified at 375×812 and 1280px.

`npx tsc --noEmit` and `npx next build` both clean (two pre-existing, unrelated errors — `layout.tsx`'s `LayoutProps` and `Logo.tsx`'s missing image module — reproduce identically on `main` and `feature/phone-emergency-legal-reviews` before this change).

**Spacing tightened, Footer cut down to legal links only — second review round, 2026-09-02:** Two more rounds of direct feedback on the live preview. First: `BookingBlock`'s section padding and inter-element margins (py-16/20, mb-4/8, gap-5/10) read as too loose — trimmed throughout (py-10/14, mb-2/3/5/6, gap-3/8), and both `Footer` variants' padding tightened the same way (py-8→py-6, py-4/5→py-3).

Second, larger change: a screenshot with the old mobile `Footer` circled in red — brand name, address, the repeated Book/Call buttons, and "Dental emergency" — asked to remove all of it, keeping only Privacy/Accessibility, repositioned directly below `BookingBlock`'s photo. `Footer.tsx` is rewritten from a Mobile/Desktop-split component carrying brand/address/CTAs/legal-row/copyright down to one simple block: `Privacy · Accessibility` plus the copyright line, nothing else — everything removed was already stated once in `BookingBlock` immediately above (practice name and hours/location in its text column, Book/Call/Emergency in "Quick actions"), so this stops repeating it a second time at the very bottom.

This drops item 7's footer-link acceptance criterion (`backlog.ts` status moved `done` → `partial`, noted inline) — `/emergency` is no longer linked from the footer, only from BookingBlock's Quick Actions pill and the mobile hamburger menu, both still one tap. Item 12 (Privacy/Accessibility in the footer) is unaffected — both stayed. Verified at 375×812: the photo is immediately followed by the two legal links and copyright, no brand/CTA repeat. `npx tsc --noEmit` and `npx next build` both clean.

**"What we treat" rebuilt and the same-day-appointment claim removed — items 62, 64, 65, 66, 67, 2026-09-03:** the first implementation pass from the 2026-09-03 recorded homepage review.

- **Item 62 (launch-blocking, legal) — done.** "Same-day appointments" is removed, not softened: Akash called the claim inaccurate, and nothing replaced it while item 15 (online booking) is blocked. Swept from five places, one of which the item had not listed — the site's own meta description in `layout.tsx`, which is what search results would have kept showing. Same-day *crowns* is untouched and now leads the capability half of the trust block. The emergency FAQ answer and BookingBlock's "same-day slots are often available" are both process-first now; the practice's live-site wording ("there is always an on-call dentist", "in most cases… the same day you call") is logged to item 63 rather than reinstated.
- **Item 64 — partial.** In-network leads the trust block and a modern/digital row is second, per the review's stated rationale that most patients here are insurance-driven. Three rows shipped, not the seven the review named: the other four each need an answer from item 63, and this item's own rule is that every row traces to a verified fact.
- **Item 65 — decided and done.** Akash delegated the taxonomy call with a defensibility requirement, so it was made from a market read of the three nearest competitors plus blueprint v2, and recorded as a `decision` on the item. **Restorative stays top-level** (the local norm, and Dr. Dubey's own specialty) but in patient language with the clinical term as a subtitle (blueprint §6). **Emergency is a shortcut, not a category** — `/emergency` stays the single source for urgent guidance. The review's fifth category, Botox for headaches/TMJ, is **held back** until item 63 confirms it: it appears nowhere on the practice's own live site.
- **Item 66 — partial.** All four categories rewritten from scratch in patient language, each with its sub-services and its own Schedule action (92×44px, all to `/contact`), plus the emergency shortcut below the stack. The section grew from 2.48 to 3.57 screens at 375px after three rounds of trimming — recorded, not hidden; the one remaining cut is dropping the mobile card photos, which is Akash's own uniform-tile call to make.
- **Item 67 — done.** The office blurb's equipment list is replaced by the practice's own "Care. Skill. Choice." values language, retrieved verbatim from www.supertoothdentistry.com rather than rewritten.

**Akash answered five of the open verification questions the same day (2026-09-03), and the work landed in the same PR:** the Botox credential is **AAFE** (American Academy of Facial Esthetics) — the "AACE" reading in the review notes was a transcription artifact, and `credentialBadges` now names the academy; availability is stated as a process in his own framing ("same-day when we have it, as soon as we can when we don't"), which also restores the emergency FAQ answer to the practice's own live-site hedge — routine same-day appointments are still **not** claimed; the practice **does** treat TMJ and headache patients with therapeutic Botox and asked for it to be highlighted, so it ships both as a trust row and as the fifth "what we treat" category, releasing item 65's held-back constraint; and "bring all in" on scope, so root canals, extractions, implant restorations, dentures, metal-free fillings, whitening and cosmetic consultations are all named on the cards. **Still open, and why item 63 stays launch-blocking:** the AAFE and AACA certificates are not on file, the carrier list has not been re-checked for currency, the downtown/Queen Anne wording is unanswered, and instant online booking is claimed by Akash but has no URL — the practice's own /appointment page is a request form, so nothing on the site claims instant booking until a real scheduler link exists.

**Credential list restyled per an external design spec — PR open, not yet merged, 2026-09-02:** Akash supplied `training-affiliation-spec.md`, a portable design spec (8 options, generic Penn/Weill-Cornell example content, its own navy/teal/cream token set) diagnosing a common failure mode in "training affiliation" sections — floating logo cards with uneven visual weight and low information-per-pixel. Its own explicit recommendation, Option 02 ("Institutional Wordmark List"), is "the safest premium implementation... resolves every diagnosed problem while sidestepping trademark exposure" — text rows instead of cards/pills, institution name in serif, one muted context line beneath, a hairline divider between rows (not around them).

Implemented that structure on top of the pill design below, per Akash's explicit direction on two open questions: (1) scope — Option 02 only, replacing the pills, not a multi-option comparison; (2) **color tokens — keep this site's locked palette (Warm Ivory/Terracotta/Espresso/Sand), not the spec's own navy/teal/cream** (CLAUDE.md's base-color-token guardrail — confirmed with Akash rather than silently applying colors read from a file). Institution name → `font-display` (Fraunces, this site's existing locked display font), context line → muted Inter, divider → `divide-espresso/12` (deliberately not `divide-sand` — the pill version's `border-sand` was already flagged as "too mild" against this same section's `bg-sand/30` wrapper on `/about`, so the same low-contrast mistake was avoided here up front rather than needing a second fix). The existing custom icons (not real org logos, see PR #60 below) carry over as the spec's optional decorative leading monogram, `aria-hidden` since the row text already states the same thing.

**Dr. Archana's credential/training-and-affiliation badges — PR [#60](https://github.com/akashsst-code/supertooth-dentistry/pull/60), merged to `main` 2026-09-02:** the "pending confirmation" placeholders in `content.ts`'s `credentials` export are replaced with real data Akash supplied directly in chat: Certified Botox Provider (American Academy of Facial Esthetics / AAFE), AACA Gold Status Provider (American Academy of Clear Aligners), Member AGD (Academy of General Dentistry), Trained in Implant Dentistry, and Member ADA (American Dental Association). No longer rendered through `<Placeholder>` — this resolves the last open item in the Section 8 content-blocker list for Dr. Archana's credentials, and the "credentials copy" placeholder called out in the Homepage v1 status entry below.

The old flat `archana.badges`/`archana.certifications` fields (15+ years experience, University of Colorado, Invisalign certified, plus the "Certified Invisalign Provider · Certified Botox Provider · ..." summary line) are retired and folded into the same new `credentialBadges` array in `content.ts`, replacing both the old badge pills and the certifications line — one data shape instead of two, no duplication. Rendered via a new shared `CredentialBadges` component (used on both the homepage `TrustBlock` bio card and `/about`), which went through two visual passes before landing:

1. A tiled emblem-badge grid — 3 "top item" facts (experience, degree, Invisalign) larger with a terracotta ring + decorative laurel behind the icon, 5 more credentials in a denser grid underneath. Cramped into awkward 2-up cells on an actual phone ("spacing inside the badge is weird ... training and affiliations mobile looks weird" — Akash) — a grid cell's width depends on which surrounding card renders it (`TrustBlock`'s photo-split bio card vs. `/about`'s full-width card), so no single column count worked in both places.
2. A medallion list — seal-style double-ring badges (bigger + a verified-check accent for the 3 top items), one full-width row per credential instead of a grid, which fixed the cramping but read as too tall for a scan-and-move-on trust signal.

**Shipped:** compact grouped pills — 3 labeled groups (Experience & Education, Certifications & Training, Professional Memberships; `credentialBadges[].group` in `content.ts`, new field replacing the medallion pass's `featured` boolean) each rendered as a single-line row of wrapping icon+text pills, the full org name available as a native `title`-attribute tooltip instead of a visible second line. Compresses all 8 credentials into roughly a third of the medallion list's height ("compress ... near 1/2 page, almost like pills ... grouped with few items logically together" — Akash). New icons added to `icons.tsx` across these passes: graduation cap, aligner/tooth, syringe, dental implant (the laurel from pass 1 was removed again once the medallion/pill passes replaced it).

**Appointment-form confirmation window — item 9, status `done`, 2026-09-01:** `AppointmentForm.tsx` already had labels above fields, blur-triggered validation, a submit-time error summary, per-field errors linked via `aria-describedby`, and a genuinely optional "Additional details" field — most of item 9's scope predates this pass. The one gap against its acceptance criteria was the success state: it said the practice would reach out "shortly," with no actual callback window, and that item explicitly requires the window be "verified with Akash" rather than invented (this repo's no-unverifiable-claims rule). Akash confirmed **within 1 business day**; the success message now states that and adds a "need us sooner? call the number below" line pointing at the existing call fallback. No other logic changed. `npx tsc --noEmit` and `npx next build` both clean.

**Emergency reachability without a bottom bar — item 45, header treatment tried and rejected, back to `not-started`, 2026-09-01:** Akash's 2026-08-30 ruling declined a sticky bottom bar but left the underlying job open — item 7 makes `/emergency` P0, yet the fixed header carried Call, Schedule and the hamburger, with no emergency entry. Built a dedicated Dental Emergency link into the header on every route (mobile: icon-only `bg-alert` circle beside Call/Schedule/Menu; desktop: a matching badge before Book Appointment, icon-only at `md` and labeled at `lg`), reusing item 46's already-approved undiluted-alert-colour treatment. Fitting a fourth mobile control also required hiding `Logo`'s wordmark across the whole mobile range and making the desktop badge icon-only below `lg`, both to avoid overflowing the header at 320px and 768px (both measured directly, not guessed) — full detail in `backlog.ts` item 45's scope history.

Akash reviewed and didn't want emergency in the top nav at all. Reverted in full — `Nav.tsx` and `Logo.tsx` both back to their pre-item-45 state, no trace of the header entry left in the code. Item 45 is back to `not-started`: reachability currently falls back to the hamburger-menu link only (item 7's original minimum), which doesn't satisfy this item's one-tap acceptance criterion. Item 46 (emergency colour token) drops back to `partial` too — it was only marked `done` pending this rollout, which no longer stands; still just the one BookingBlock call site. Header placement is now a tried-and-rejected option for item 45, not an open one — next candidate (in-page affordance, footer, something else) needs Akash's steer.

**Phone/NAP fixed, /emergency shipped, /privacy and /accessibility drafted, real reviews landed — items 3, 7, 12, 13, 2026-09-01:** Akash resolved four of the outstanding launch-blockers in one pass: the correct phone number, the after-hours reality for item 7, and a direct instruction to pull real content for items 12 and 13 rather than continuing to wait.

**Item 3 (`done`):** Akash confirmed `(206) 593-3131` — checked directly against the live Google Business Profile listing ("Supertooth Dentistry," 133 Queen Anne Ave N Unit A), which shows the same number, and the same Tue–Fri 7:00–4:30 hours already in `content.ts`. `contact.phone` updated; every `tel:` link sitewide already read from that single source, so no other file needed touching. A `src/` grep for phone-shaped strings turns up no second number.

**Item 7 (`done`):** Akash's confirmed after-hours reality is "call, or schedule now" — no answering service, no voicemail-specific promise. Shipped `src/app/emergency/page.tsx`: three-tier ADA-sourced guidance (911/ER red flags, call-us symptoms, while-you-wait first aid), Tier 1 first in DOM and visual order, a one-tap `tel:` control plus a "Schedule now" link, and the after-hours line stated with no invented response-time promise. Verified at 375×812 that Tier 1 renders well within the first viewport alongside the call control. Linked from the mobile hamburger menu and the footer (both new), and this also closes the interim gap the item-48 entry below flags: `BookingBlock.tsx`'s "Dental Emergency" quick-action badge now points to `/emergency` instead of the `/contact` placeholder it shipped with. Item 45 (a more deliberate emergency-reachability/visual-distinction pass) is unaffected and still `not-started` — today's nav/footer links are the minimum item 7 itself asked for, not that item's fuller treatment.

**Item 12 (`partial`, not `done`):** `/privacy` and `/accessibility` are both live and linked in the footer, both render as real HTML (no PDF) at 375px, and `/accessibility` has a one-tap `tel:` feedback route and states WCAG 2.2 AA as the target while being honest that item 14's audit hasn't run yet. This stops short of closing the item on purpose: `/privacy`'s text is an explicitly labeled **draft** (`content.ts` `privacyPolicy`, informed by comparable practice sites and the HHS model-notice structure, with a visible "pending review" banner on the page) — it is not the practice-approved or counsel-reviewed text this item's own acceptance criteria requires, and this item's own scope names writing that text ourselves as out of scope. Still needed: Akash/counsel sign-off on the privacy text, and an explicit call on SMS disclosure if that ever applies.

**Item 13 (`done`):** pulled the current top three reviews directly from the live Google Business Profile — Elise T., Vandana S., Berri R. (first-name + last-initial, the locked HIPAA format), quotes trimmed to the last complete sentence available before Google's own truncation point rather than cut mid-sentence. `<Placeholder>` wrapping removed from `TestimonialsSection.tsx`'s rating/count/quote/name now that they're real. Rating stayed 4.9; count corrected 487 → 427 against the live listing. Expanded to five reviews the same day at Akash's request (Dawn B., Karthik B. added), and the "What patients are saying" header's Google rating badge — previously a bare 16px icon easy to miss against the dark section — was resized and given a white roundel so its brand colors read clearly. A Google-widget-style card variant (avatar-initial roundel, real "Local Guide · N reviews" line, small G mark) was also built as a side-by-side comparison against the original custom quote-card design, kept in this site's own translucent-on-espresso color rather than a white surface per Akash's call.

`npx tsc --noEmit` and `npx tsx scripts/check-backlog.ts` both clean (the script's one pre-existing failure, item 57's reference count, is unrelated to this pass).

**Three clear doors on the homepage — item 48, status `partial`, merged 2026-08-31:** Akash asked to fix item 48 (Hero: Book · New patient · Emergency). The homepage previously offered one CTA (Book Appointment) plus a bare phone link, so a new patient or someone in pain had to work out their next step from the nav — the gap item 48 exists to close.

Went through several rounds of direct feedback the same day before landing on the shipped version — full blow-by-blow (including the discarded Hero three-door stack, the color-clash and "hidden" missteps on Dental Emergency's badge) is kept in `backlog.ts` items 46 and 48 rather than summarized away. **Final state:** Hero stays the original two-CTA pattern (Book Appointment + Call) — a three-doors-in-Hero version was tried and reverted as overwhelming for a small boutique practice mainly focused on winning new patients. `BookingBlock.tsx`'s "Quick actions" row is now three items lined up together — **Book Appointment**, **Call**, and a compact **Dental Emergency** badge (undiluted `--color-alert`, deliberately smaller than the two primary pills, `items-center` aligned) — and **New Patient** sits on its own quiet line below Office hours/Location.

**Verified at 375×812 and 1280px:** Quick actions' three items align on one row at desktop width (confirmed via `getBoundingClientRect()`) and wrap cleanly at mobile widths; every link resolves to a real, already-built destination.

**Known gap, not fixed here:** item 48 depends on item 7 (`/emergency`), which is still blocked on Akash confirming the after-hours reality. Rather than leave the Emergency link pointing nowhere, or invent an after-hours claim to unblock item 7 early, Akash approved routing it to `/contact` (the existing appointment-request form) as an interim destination — real, but not the tiered non-diagnostic guidance item 7 calls for. Both `backlog.ts` items 46 and 48 are left at `partial`, not `done`, to keep that honest; re-point once item 7 ships.

**Cross-page journey walked — item 55, status `done` as of 2026-08-31, PR open, not yet merged:** Item 27 walks the homepage; item 31 tests three journeys once everything ships. Nothing reviewed the site as a connected journey across its six routes (/, /about, /services, /insurance-new-patients, /contact, /backlog) — the gap this item fills, since most patients enter mid-funnel rather than at the homepage.

Built the full inbound-link map by grepping every internal `href` in `src/app` and `src/components`: only `/`, `/contact` and `/backlog` appear as literal link targets anywhere in page or component content — `/about`, `/services` and `/insurance-new-patients` are reachable exclusively through the persistent `nav` array Nav.tsx renders. Because that header is `fixed` (not `sticky`), it stays pinned at every scroll position on every route, so the locked ≤2-tap-from-Home-and-footer guardrail holds without the footer needing its own link list — verified live from `/contact` scrolled to the bottom. Zero orphan routes (all six are nav-linked; `/backlog` is the one deliberate noindex exception the item allows) and zero dead ends (every route carries a visible Book Appointment CTA when landed on directly). No horizontal scroll at 320px on any of the five patient-facing routes.

Of the nine researched funnel stages, eight have a serving surface; post-visit has none, but that's already tracked as item 22. Two real gaps had no existing owner and were newly logged: **item 58** ("Add related-page cross-links between Insurance, Services and About," P1) and **item 59** ("Open question: native 'Get Directions' link vs. the on-page map embed," P2) — the latter filed as a `conflict` rather than fixed unilaterally, since both map components deliberately sandbox their embed against navigating away (a past explicit call to keep visitors on-page). Of the 12 researched patient scenarios walked end to end, 9 complete cleanly; the other 3 stall on items 58, 59, 42 and 43, all already tracked or newly logged here. Full findings in item 55's `evidence` field in `src/lib/backlog.ts`.

**Items 58 and 59 — built same day, 2026-08-31, both `done`:** Akash reviewed the two logged gaps and asked for both. Item 58: one contextual link added on each of `/insurance-new-patients`, `/services` and `/about` (→ services, → about, → insurance respectively), each a secondary line above that page's existing Book Appointment CTA so the primary action stays visually first. Item 59: ruled `approved` (`decision` recorded on the item) — added `contact.mapDirectionsUrl` in `content.ts` (a plain Google Maps search-URL per that item's own reference) and a real "Get Directions" anchor rendered outside each iframe, so the embed's own `sandbox` attribute and on-page-preview behavior are unchanged. In `HeroAddressMap.tsx` it sits inside the expanded map panel next to the close button; in `LocationMapSection.tsx` it sits directly under the address. Both link out at a 44×44px tap target, confirmed via `getBoundingClientRect()` in the browser. `npx tsc --noEmit`, `npx tsx scripts/check-backlog.ts` and `npx next build` all clean; no horizontal scroll at 320px; console clean.

**Mobile-first type and spacing scale — item 37, worked this pass, status `partial`, PR open, not yet merged:** the locked design tokens defined colour and font family but never a type scale, so sizes were being chosen per component — exactly how a "16px minimum" rule drifts (Logo's "Dentistry" wordmark and two insurance-badge labels had quietly slipped to hardcoded 10–11px). Fixed at the source rather than per-component: `globals.css` now overrides Tailwind's own `--text-xs` through `--text-7xl` tokens through the same `:root`-variable + `@theme inline` indirection already used for the locked colour tokens, so every existing `text-*` utility across every component picks the scale up automatically, mobile-first (base 17px, fine-print floor 14px, both above the required 16/14px floors) with a `@media (min-width:1024px)` block enhancing the same named roles upward for desktop — no per-component rewrite needed. Hero's `h1` line-height raised from 1.1 to 1.2 to clear the 1.2–1.3 heading floor. The three hardcoded sub-14px labels were moved onto the new `text-xs` token.

Verified against the running dev build, not just read off the CSS: body/inputs render at 17px, no horizontal overflow at 320px on any of the 5 patient-facing routes, contact-form inputs measure 17px, and `html{font-size:200%}` produces no clipping (WCAG 1.4.4/2.2). Spacing needed no change — Tailwind's existing 4px base scale already lines up with the locked 4/8/12/16/24/32/48/64/96 scale.

Left `partial`: `/backlog` (the internal, noindex review tool) still has ~15 hardcoded sub-14px labels, deliberately untouched since this item's own story is scoped to patient-facing pages and another session had that file under active edit. A pre-existing, unrelated Nav.tsx bug was also surfaced and logged separately (item to follow) rather than fixed here — the header logo visually clips behind the phone-icon button at exactly 320px, confirmed independent of font size.

**Homepage mobile-flow review — item 27, status `done` as of 2026-08-31 (merged):** Akash asked whether anything in the backlog checks the mobile homepage flow page-by-page, feature-by-feature. Audited: nothing did. Items 16/17/19 *add sections to* the homepage and item 14 is a sitewide accessibility audit, but no item tested the homepage as a continuous mobile journey — the gap that matters most, since Section 2 above makes the homepage the full conversion journey and the site is single-page-led. A page can pass every per-feature test and still fail as a sequence.

Item 27 scores **42.0/50 — third-highest in the backlog**, P0, Wave 1. A ten-minute measurement of production at 375×812 (2026-08-30) seeded it with real findings, and this pass acted on them:

- **12.4 screenfuls** of scroll at 375×812, unchanged (trust block 2.83 screens, services 2.55).
- **All five interactive elements below the locked 44px minimum are now fixed**: `+ more` (was 40×16) in `InsuranceTeaser.tsx`, the hero address button (was 288×16) in `HeroAddressMap.tsx`, and both `Schedule this offer` links (was 146×24) in `NewPatientOffersBlock.tsx` all gained the `tap-target` utility class. The phone link (was 102×17) in `FAQSection.tsx`'s emergency answer was restructured rather than just padded — it was genuinely mid-sentence ("Call us at *(206) 687-7571* — {answer}"), and padding an inline link to 44px tall breaks a flowing paragraph's line height. Pulled it out into its own tappable row above the answer text instead: reads as "Call us at (206) 687-7571" (191×44, real tap target) followed by the answer as its own line — cleaner than the old inline sentence, not just compliant.
- **In-content booking CTAs cluster at screens 0–2.1, then nothing until 8.7** — a 6.6-screen gap spanning testimonials, services and location. **Re-verified, not just re-asserted**: the fixed header genuinely persists at `top: 0` with both the Schedule and call controls measuring exactly 44×44px at screen depths 3/5/8/11.
- **Redundancy checked**: no accidental repeated photo/CTA within two screens. The office-photo carousel's repeated `office-*.webp` sources at the same scroll position are deliberate clone-slides for seamless infinite-loop scrolling, not a bug.
- **No horizontal overflow** at 320px or 375px, at any scroll depth.
- **Desktop pacing (1280px, run after the mobile pass per the locked mobile-first ordering)** surfaced one real outlier: `ServicesSection` is 4.07 screenfuls at desktop widths — the tallest section on the page, well past the ~2.5-screen guideline (mobile's version of the same section is a reasonable 2.55). Trimming it is a grid-density change, not a mobile-flow repair, so it's logged as its own item rather than fixed here: **item 56, "Rebalance ServicesSection height at desktop widths," P2.**
- **Core Web Vitals, measured 2026-08-31** against the deployed Vercel preview with throttled mobile Lighthouse (4x CPU slowdown, simulated slow 4G — the earlier pass's tooling had no throttle control, hence the gap): LCP came in at 2.4-3.6s across three runs (median 2.9s), above the 2.5s budget; CLS was 0. Root-caused and fixed: `HeroCarousel.tsx`'s LCP image used the `priority` prop, which Next.js 16 deprecated in favor of `preload` — and unlike the old prop, `preload` alone no longer implies `fetchPriority="high"`. Confirmed via the production HTML that the attribute was genuinely missing from both the image and its preload link before the fix. After switching to `preload` + explicit `fetchPriority="high"`, re-measured LCP at 2.4-3.2s (median 2.6s) against this fix's own preview — real improvement, though preview-vs-preview variance means this is directional rather than a clean lab comparison. The Lighthouse trace showed the fetchPriority fix solved the discovery/network side of LCP but surfaced a separate, unexplained ~960ms "element render delay" now eating most of the remaining budget — logged as its own item (**item 57, "Cut HeroCarousel's ~960ms LCP element-render delay," P1**) rather than chased further in this pass, since root-causing it needs dedicated investigation. This also updates **item 38** ("Performance and Core Web Vitals budget," the sitewide item this measurement was really scoped under) from `not-started` to `partial` — first real baseline recorded, one fix landed, one miss logged with item 57 as owner.

With that finding logged, all five of item 27's acceptance criteria are met (tap targets fixed, booking reachable at every depth, scroll depth recorded with the one outlier logged as item 56, Core Web Vitals measured and recorded at 375px throttled, and every other finding fixed or logged) — item 27 moves to `done`.

The five tap-target defects were the direct answer to why this item was needed: item 14 would eventually have caught them, but item 14 is gated behind items 1–13, so they'd have sat in production meanwhile. Item 27 was unblocked and actionable today, hence working it out of order ahead of items 1–13.

**Item 57 ("Cut HeroCarousel's ~960ms LCP element-render delay") — investigated 2026-08-31, closed `done`, no code change:** Before writing any fix, re-ran the same Lighthouse measurement 8 times against production to confirm the ~960ms figure was real and not a fluke. It wasn't: every one of the 8 runs came back at 28-80ms (median 56ms) — over 10x smaller than the original reading, and consistent enough to rule out ordinary noise. Traced the likely cause: `uptime` showed a load average of 4+ on the machine these measurements run from, with several concurrent Claude Code sessions and dev servers competing for CPU at the time of the original reading — exactly the kind of contention that can stall a headless Chrome trace capture independent of anything the site's code does. Confirmed with a second, more rigorous method: `--throttling-method=devtools` (which genuinely throttles the network/CPU during capture) gave elementRenderDelay of 10-19ms across 3 runs, and — unlike `--throttling-method=simulate` — its four breakdown phases actually summed to the reported total LCP each time, which `simulate` mode's didn't on this host. A real, reproducible near-1-second main-thread stall would have shown up under genuine throttling too; it didn't. Conclusion: the original measurement was a one-off artifact of a busy shared host, not a defect in `HeroCarousel.tsx` or `ViewportHero.tsx` — no code touched.

This also resolves **item 38** ("Performance and Core Web Vitals budget") from `partial` to `done`: re-measuring the full budget against production with the now-preferred `devtools` throttling method gave LCP 2110-2123ms, CLS 0, TBT 20-30ms, Performance score 0.95-0.98 across 3 runs — every threshold cleanly met, superseding the earlier `simulate`-mode reading that had looked marginal. **Going forward, prefer `--throttling-method=devtools` over `simulate` for any Core Web Vitals check in this repo** — `simulate`'s modeled numbers didn't reconcile with themselves on this measurement host, while `devtools`'s did, consistently.


**Backlog scored, re-prioritized, referenced and made testable — PR open, not yet merged:** the backlog merged in PR #39 asserted its P0/P1/P2 bands from the research. This pass replaces assertion with a model, per Akash's ask.

Every one of the 26 items now carries a **weighted 5-factor score** out of 50 — new-patient conversion (×3, the locked project goal), risk if skipped (×2.5, because healthcare failure modes are surprise bills and unsafe guidance), patient reach across the 12 researched scenarios (×2), cheapness (×1.5, a tiebreaker not a driver) and readiness (×1, so being blocked lowers sequence rather than importance). Bands are re-derived from the score (P0 ≥ 33, P1 ≥ 26), with two pin types that can only ever promote: `legal` for the non-negotiable items (2, 6, 7, 12, 13, 14) and `dependency` for pure enablers (item 5 alone).

**Five items moved: 14/8/4 → 15/5/6.** Dental anxiety content (35.5) and the cost/financing explainer (34) were promoted P1 → P0 — anxiety is the best-evidenced patient need in the research, is Small, and is effectively unblocked. `/services` dropped P0 → P1 (29.5) because the urgent half of it, a nav link that 404s, is fully handled by item 1. Per-service pages (22) and aftercare/records (21.5) dropped to P2. Full before→after table with rationale in the research doc, Section 19a.

Two results are worth Akash pushing back on. **Online booking scores 32.5 and stays P1** despite maxing both conversion and reach — Large effort plus being fully blocked on Tab32 pull it under the line. That's the model working as intended, and the moment Tab32 unblocks, readiness goes 1 → 4 and it becomes P0 at 35.5. **PageShell scores 22**, which is honest — it's pinned purely because five P0 items depend on it, and recording that as a pin rather than inflating its score keeps the model trustworthy.

Each item also gained **2–3 references** for what good looks like (60 total, deepest on P0 — spanning dental practices, health systems, GOV.UK/NHS design systems, W3C/Schema.org, and ADA/HHS/CMS/ICO), each with a link, what's specifically good about it, and what to copy *versus deliberately avoid*. And each gained an **agent-executable test scenario**: preconditions, numbered steps tagged with the tool to use (`browser`/`shell`/`validator`/`manual`) and the expected result of each, explicit pass criteria, and gotchas that would otherwise produce false passes — written so an LLM can run them unattended and loop until green.

**Mobile-first correction (same PR, after Akash challenged it).** The first pass of the above was written desktop-first without noticing. Measured: only **9 of 129 test steps** mentioned mobile at all, **13 of 26 items — including 5 P0s — had zero mobile-aware steps**, and **23 of 26 items had no reference that considered mobile**. That's mobile bolted onto desktop-first work, on a project whose own build principles require the mobile experience to be complete rather than a reduced desktop version.

Fixed structurally rather than cosmetically. Test steps now run at **375×812 by default** — an untagged step is a mobile step; desktop is an explicitly tagged confirmation pass that only runs after mobile passes; `any` marks steps with no rendered surface. Every item carries a **mobile gate** whose criteria must hold at 375px *before any desktop check counts* (98 criteria across 26 items). Every one of the 60 references carries a mobile assessment. Coverage went from 9/129 to **101 of 148 rendering steps at 375px**, against 18 at desktop.

The reference audit was the most useful part, because several are good on desktop and poor on a phone — and saying so beats the original praise: MedStar's wayfinding PDFs are near-unusable on the device where arrival instructions actually get read; Humana's plain-language policy has the right register but a length that buries the key distinction below the mobile fold; CMS's machine-readable price files are technically transparent and practically unreadable on a phone.

It also surfaced findings that desktop-first testing would have missed entirely — most seriously on `/emergency`, where a three-column tier layout puts everything above the fold at 1280px and pushes Tier 1 red-flag detail below it at 375px. A red flag below the fold on a phone is a patient-safety failure, not a layout nit. Also: WCAG 2.2's new criteria (Target Size, Dragging Movements, Focus Not Obscured) are mostly touch concerns and our two draggable carousels plus fixed overlay nav sit right on them; a booking calendar grid at mobile width produces sub-30px targets; and a resized desktop window never reproduces the on-screen keyboard.

`scripts/check-backlog.ts` now enforces all of it — it fails if an item lacks a mobile gate, if a reference lacks a substantive mobile note, if an item with a rendered surface has no 375px step, if desktop steps outnumber mobile ones, or if the first viewport-bound step isn't mobile.

`scripts/check-backlog.ts` guards the model: it re-derives every band from the scores and fails on drift, and enforces the structural invariants (unique ids, scores in range, effort consistent with the cheapness score, a rationale on every moved or pinned item, minimum reference and test-step counts, no test step with an empty assertion). Run with `npx tsx scripts/check-backlog.ts` — no devDependency added. It caught six genuinely thin entries on its first run, all since fixed.

**Patient-needs research + prioritized backlog — merged to `main` via [PR #39](https://github.com/akashsst-code/supertooth-dentistry/pull/39):** first-principles research into what patients need from a family dental practice serving the Downtown Seattle catchment, deliberately conducted *without* auditing the current site first, then reconciled against what's actually shipped. Full report in `docs/supertooth-patient-needs-research.md`; the actionable slice is `src/lib/backlog.ts`, rendered at `/backlog` and linked from the mobile hamburger menu.

Four findings that change what gets built next:

- **Three of four primary nav links 404.** `nav` in `content.ts` links to `/services`, `/about` and `/insurance-new-patients`; none of those routes exist. Highest severity-to-effort item in the backlog, and it needs nothing from Akash.
- **The practice is in Lower Queen Anne / Uptown, not the downtown core.** Downtown-working patients are a real segment, but they will never walk past the office, so arrival content (transit, parking, entrance, "Suite A") is conversion infrastructure here rather than footer trivia. The Seattle Center Monorail reaching Westlake in roughly two minutes is the most useful local fact available and no reviewed competitor uses it.
- **Insurance ambiguity is the highest-consequence content risk on the site.** ADA guidance documents patients conflating "accepts your insurance" with "in-network with your plan", including a case where a practice site listed carriers the insurer's directory didn't corroborate and the patient was balance-billed. `insuranceCarriers` currently ships six names this repo's own comments flag as unconfirmed — Section 7's no-unverifiable-claims rule already covers this; the research supplies the cost of ignoring it.
- **Dental anxiety is the best-evidenced patient need found and the site says nothing about it.** Peer-reviewed prevalence sits near 19% of adult dental patients (≈15% high fear), with prevalence approaching 49% among patients presenting for *emergency* care — so the urgent path and the anxious path are frequently the same person. The cheapest useful version is one non-judgmental sentence plus an optional "anything we should know?" field on the appointment form.

26 items across three priorities (14 P0 / 8 P1 / 4 P2), sequenced into five waves, with a marked "patient-ready line" after item 14. **10 are blocked on Akash** — Section 22 of the research doc lists all 20 claims needing practice verification before launch, and Section 23 the 12 open questions. Six things are recorded as *deliberately not built* (symptom checker, cost calculator, patient portal, chatbot, personalization, live "open now" status) so those decisions don't get re-litigated.

`/backlog` is an internal working artifact on a patient-facing site, so it's `noindex, nofollow` and is deliberately **not** added to the `nav` array — that array is the patient-facing wayfinding surface locked in `supertooth-navigation-requirements.md` and it also feeds the desktop nav. It renders instead as a separate, de-emphasized row at the bottom of the mobile menu, below both patient CTAs.

**Hero redesigned photo-first, full-bleed — PR open, not yet merged:** replaces the split video/text layout (Hero v2, locked 2026-08-22) per Akash's ask, referencing 2thstudio.com's clean full-bleed pattern: "make the photo occupy most the screen, and bleed the bottom part in." `HeroCarousel.tsx` now fills the entire hero section edge-to-edge (was a 60%-wide side panel next to a solid espresso text panel) at every breakpoint; eyebrow/headline/body/trust-strip/CTAs are overlaid directly on the photo, bottom-anchored, over a bottom-up scrim (`from-espresso/95`) for text contrast. A second, shorter gradient below the content fades the photo into the page's warm-ivory background so TrustBlock reads as a continuation of the hero rather than a hard-edged next section (the "bleed"). The carousel's own internal scrim was removed since Hero.tsx now owns legibility contrast for the whole section (it was the only place HeroCarousel is used). Section 1's locked requirement — insurance signal + both CTAs visible on load, not buried — still holds; they're just overlaid on the photo now instead of living in a separate side panel. `Hero.tsx`, `HeroCarousel.tsx`.

**Hero carousel slowed and rebalanced toward Dr. Archana — PR open, not yet merged:** two rounds of feedback on the hero reel (merged earlier via [PR #20](https://github.com/akashsst-code/supertooth-dentistry/pull/20)): it was cycling too fast to register, and the doctor wasn't reading as prominent even though her photo led. `HeroCarousel.tsx`'s pace slows from 2.2s hold / 600ms crossfade to 4.5s hold / 1.2s crossfade, and the random opening-photo behavior is removed — it now always opens on `archana.webp` (her studio headshot, the most polished/trustworthy of her photos) and plays a fixed order instead.

`heroPhotos` in `content.ts` grows from 5 to 7 and is reordered so Dr. Archana leads and closes the sequence (opening headshot, then a second candid of her, then the new outdoor candid below) with the team/staff photos grouped in the middle rather than scattered — "keep the doc in focus" while the middle stretch still shows it isn't a one-person practice. `archana-profile.jpg` (previously used only in TrustBlock's bio card — the real candid from an ADA event) is now also in the hero rotation, plus one new photo Akash supplied 2026-08-29 (`archana-candid-outdoor.jpg`, a candid portrait, resized/compressed via `sips`).

Akash also supplied two more photos for consideration (a Yellowstone/hiking photo and an evening-event photo) with a request to show "nurses and patients" in the mid-carousel photos. Neither of those two went in — both are personal/vacation-style photos rather than practice photos, confirmed with Akash before building. No real patient photos exist yet either, and none were staged or mislabeled to fill that gap (HIPAA / no-unverifiable-claims, Section 7 below) — the "mid" slots are filled with the existing real staff photos (`team-group.jpg`, `front-desk.jpg`, `team-itero-scan.jpg`) instead. Real patient photography (with consent) and any additional practice-context photos of Dr. Archana remain open follow-ups.

**Homepage FAQ section added — merged to `main` via [PR #28](https://github.com/akashsst-code/supertooth-dentistry/pull/28):** resolves the Section 7 compliance-checklist item "Schema markup: LocalBusiness / Dentist / FAQPage" and "FAQ content structured as direct Q&A pairs (AEO-ready)" for the first time in this build. New `FAQSection.tsx` — a 9-item +/- accordion, right after `NewPatientOffersBlock` and before `BookingBlock` (resolves last-minute objections just before the booking ask, without reordering anything else already locked above it) — plus a matching `FAQPage` JSON-LD block emitted from the same `faqs` array in `content.ts`, so the visible accordion and the structured data can't drift apart. Accordion interaction (button + rotating plus/minus icon + `grid-template-rows` open/close transition) reuses the same pattern Akash asked for on `InsuranceExpandCard.tsx` (a competitor-site reference), reimplemented locally rather than shared since this is a plain list of independent items, not that component's single insurance-card layout.

Section 2's original spec put FAQ on a per-service-page template; those pages (`/services`, `/about`, etc.) aren't built yet, so this ships as one general-practice FAQ on the homepage instead — same AEO goal, no template dependency. Can split into per-service FAQ blocks once those pages exist.

Content sourced from the practice's own live site, www.supertoothdentistry.com — its "frequently asked questions" section, verified against the same address/hours already confirmed real elsewhere in this file (exact match) — same trust tier as `contact`/`archana` in `content.ts`, which already cite "the practice's existing site" as their source; this is that site's current domain. Rewritten into concise direct-answer form for AEO rather than copied verbatim. Per the no-unverifiable-claims rule (Section 8 below), three things from the source site were deliberately left out rather than reused:
- The source's stated 10% senior discount didn't reappear on a second fetch of the page, so it's dropped rather than risk stating a discount that isn't current.
- Its SMS Privacy Policy blurb is a legal-disclosure item, not an FAQ answer — belongs with this section's own "Privacy policy and required disclosures present before go-live" checklist item, not something to improvise here.
- Specific new-patient/Invisalign offer amounts and named insurance carriers aren't repeated in the FAQ text — both already live in `offers`/`insuranceCarriers` in `content.ts`, each flagged unconfirmed and rendered through `<Placeholder>` at their one real display site; the FAQ points to those sections instead of restating the numbers a second time.

The emergency-visit answer also deliberately has no phone number baked into `content.ts` — the source site shows **two different** main-line numbers depending on the page section fetched ((206) 593-3131 on one pass, matching the top-banner CTA number already flagged as a discrepancy when `contact.emergencyPhone` was removed in the "Drop emergency line" commit), which conflicts with the (206) 687-7571 already confirmed real elsewhere in this file. Rather than guess or reintroduce a second unconfirmed number, `FAQSection.tsx` interpolates the one already-confirmed `contact.phone` value directly — single source of truth, and worth Akash confirming which number is actually correct before launch, same open flag as before.

**New-patient offers rebuilt as a swipeable card carousel — PR open, not yet merged:** `NewPatientOffersBlock.tsx`'s two-up static grid is replaced with a swipeable carousel (Akash's reference: a specials section showing one large rounded card with the next peeking at the edge). Swipe itself is native `overflow-x-auto` + `snap-x`/`snap-mandatory` — no hand-rolled pointer-drag logic needed, unlike `OfficeCarousel.tsx`'s continuously-animating reel, since this is a plain user-driven scroller. Chevron buttons (desktop) and dot indicators (both breakpoints) give non-touch/positional control via `scrollIntoView`, kept in sync with whatever got the user there (swipe, drag, or button click) via an `IntersectionObserver` on the scroll track. No autoplay, so none of `OfficeCarousel.tsx`'s WCAG 2.2.2 pause-control machinery applies. Cards keep the locked "half page picture and 1 line text offer" content call (offer copy stays one line, still through `<Placeholder>` pending confirmed pricing) but are restyled as standalone rounded Sand-surface cards with an eyebrow label (`offers.*.label`, new field in `content.ts`) and a circular photo, closer to the reference look than the old half-card rectangle split.

**Hero photo carousel — merged to `main` via [PR #20](https://github.com/akashsst-code/supertooth-dentistry/pull/20):** the Hero's single "video-styled" photo (`ClinicVideo.tsx`, Dr. Archana's studio headshot with a Ken Burns zoom and a "real video coming soon" play badge) is replaced by `HeroCarousel.tsx`, a 5-photo auto-advancing crossfade of real team/office photos Akash supplied directly in chat — the ask was to make the hero read as "more than just the dentist," showing other real people in the clinic, without pretending to be an actual video. `archana.webp` (the original hero photo) is one of the 5 slides; four new photos were added to `public/team/`: a group shot of the team together (`team-group.jpg`), a front-desk team member at her desk (`front-desk.jpg`), a second candid of Dr. Archana Dubey in the office (`archana-candid.jpg`), and a team member reviewing an iTero digital scan (`team-itero-scan.jpg`). Same Ken Burns zoom (`globals.css` `.animate-slow-zoom`) as before, applied continuously to all 5 stacked images so crossfades never pop.

Shipped with dots, a caption badge, and a pause/play control initially, then simplified per Akash's direct follow-up call ("just have photos run... this is just pure trust building, no user can pause or click"): no visible caption, no dots, no pause control — a purely passive background reel with zero interactive surface. Auto-advances every 2.2s via `setInterval` (down from an initial 5s), crossfade shortened to 600ms to match the faster pace, and the opening photo is now randomized per page load (chosen post-mount so server/first-paint markup still matches, avoiding a hydration mismatch) instead of always starting on `archana.webp`. `prefers-reduced-motion` still freezes the reel on whichever photo is showing — that's a passive system preference, not a user-facing control, so it's unaffected by the no-controls call. The repo's own locked accessibility checklist (Section 7 below) doesn't actually require a pause control for this kind of passive slideshow — that was `OfficeCarousel.tsx`'s own added rigor for a *draggable* reel, not a rule this component inherits.

Only Dr. Archana Dubey's identity is confirmed among the new photos — cross-checked against her two already-real, name-tagged photos in `content.ts` (`archana.webp` / `archana-profile.jpg`, both showing "Archana Dubey" embroidered on her coat) to confirm which person in the supplied group photo is her, since two other people also appear across the new photos without confirmed names. Rather than guess, those two are described by role/moment in their `alt` text ("A team member at the front desk", "Reviewing a digital scan on-screen") instead of a name — same gap as the `team` array's existing "Hygienist name"/"Staff name" placeholders, and avoids the no-unverifiable-claims rule (`supertooth-build-principles.md` Section 8). Swap in real names/roles once confirmed.

One of the four supplied source photos (the Dr. Dubey solo portrait, now `archana-candid.jpg`) had its actual pixel data stored sideways with no usable EXIF orientation tag `sips` would act on consistently — every `sips` resize attempt (in-place or `--out`, chained or separate) re-introduced a 90° rotation even after a manual fix looked correct pre-resize. Reprocessed with Python/Pillow (`ImageOps.exif_transpose` + `resize`) instead, which handled it correctly in one pass. Worth remembering for any future photo-processing pass in this repo: don't chain `sips -r` with `sips -Z`/`--resampleWidth` on files with any embedded orientation metadata — prefer Pillow.

**Emergency line dropped entirely, hours updated, booking/footer reorganized — merged to `main` via PR #20:** briefly landed on `main` as a confirmed real number (`(206) 593-3131`, PR #19) but reversed shortly after by Akash's own direct follow-up on this same branch — `contact.emergencyPhone`/`emergencyContact` are removed from `content.ts` again, this time with no placeholder standing in for them, since there's no confirmed number Akash wants published. `BookingBlock.tsx`'s emergency banner and both `Footer.tsx` variants' emergency-line note are removed to match — if a real, confirmed emergency contact method is decided later, it goes back in as `real: true` content, not a placeholder. `hours` also updated to `Tuesday – Friday, 7:00 AM – 4:30 PM` (was Mon–Fri 8–5). `BookingBlock.tsx`'s info row was reorganized into labeled "Quick actions" / "Office hours" / "Location" groups instead of a flat row (only the open-hours line is shown next to the booking CTA — the "Closed" row still exists in `hours` for Nav's full listing). `Footer.tsx` was trimmed to avoid duplicating `BookingBlock.tsx` immediately above it (brand, address, parking, phone, copyright only — no repeated hours/CTAs), and its mobile variant now matches desktop's light Warm Ivory theme instead of repeating the dark Espresso background, so the bottom of the page reads as two distinct sections instead of one long dark block.

**Services photos cropped to fix "elongated"/unnatural cards — merged to `main` via PR #16:** the 3 real service photos Akash supplied (same-day onlay, implant x-ray, in-office whitening) are full Instagram-post-style graphics — a big title, our own logo, and (for whitening) a Philips Zoom brand badge, all sitting above the actual before/after photos. Force-cropping those into the section's `aspect-[4/5]` `object-cover` tile either cut photos off mid-frame or crushed a whole poster into a thumbnail — read as "elongated"/unnatural (Akash, with a screenshot). Cropped each down to just the labeled before/after photos (`*-crop.jpg` in `public/services/`, same source photography — title/logo/badge removed, not new images) and gave `ServicesSection.tsx` a natural-aspect-ratio render path (using `image.width`/`image.height` from content.ts) for images that carry their own dimensions, instead of forcing every photo into the same fixed box. Originals kept in `public/services/` (unused by the site now) in case Akash wants them elsewhere (e.g. actual Instagram posts).

**Trust-block reorder, insurance section removed, services images filled — merged to `main` via PR #16:** `TrustBlock.tsx` internal order flipped per Akash's follow-up call ("move office scroll above bio") — the office-photo carousel + "about our office" blurb now come right after the differentiators, with Dr. Archana's bio card after both (previously bio led, office followed). `InsuranceBlock` removed from the homepage entirely per Akash's explicit call (`page.tsx`) — the component and `insuranceCarriers` content are kept, not deleted, as the natural fit for the dedicated `/insurance-new-patients` page once that's built. `ServicesSection`'s one card without a photo ("General & preventive care") now has a temporary Unsplash stand-in per Akash's "bring images where we don't have" call, same convention as the offer-card images below — swap for real photography later.

**Real contact info, offer-card redesign, and booking-block rework — merged to `main` via [PR #16](https://github.com/akashsst-code/supertooth-dentistry/pull/16), reviewed and approved by Akash:** first real phone/address/hours landed for `contact`/`hours` in `content.ts`, sourced from the practice's existing site (gray-rail-265889.hostingersite.com), the same source already trusted for Dr. Archana's bio — resolves another piece of the Section 8 content-blocker list. `contact.emergencyPhone` — that source showed **two different** numbers for the emergency line (a top-banner CTA and a footer-box "current patients call Dr. Dubey directly" line); shipped behind `<Placeholder>` pending confirmation, and Akash has since confirmed the top-banner number, `(206) 593-3131`, is correct — the footer-box number was wrong. No longer behind `<Placeholder>`. `contact.parkingNote` is real (Akash's own chat wording: "bus stop on the same block, parking available on nearby streets"), replacing the old placeholder in the "Proudly serving Queen Anne" card (`LocationMapSection.tsx`).

`NewPatientOffersBlock.tsx` rebuilt per Akash's "half page picture and 1 line text offer" call — each card is now a photo (half the card, full-bleed on mobile / ~50-50 split from `sm:` up, same split pattern as Dr. Archana's bio card) plus one line of offer text, still no links. Images are a temporary Unsplash stand-in (`next.config.ts` now allowlists `images.unsplash.com`) — swap for real photography later, same as `services` images.

`BookingBlock.tsx` reorganized per Akash's "clean up alignment/clutter, bring a picture for trust, fill in hours" call: added a proper emergency-line banner (replacing the old vague "need to be seen today?" line) and a two-column layout — copy/CTAs/real hours/address on the left, a real office photo (not new stock) on the right. `Footer.tsx` and `Nav.tsx` also unwrapped now-real phone/hours from `<Placeholder>`, and `Footer.tsx` gained a small one-line emergency note in both variants — deliberately compact so it doesn't duplicate the larger BookingBlock banner. No link-list columns were added to the footer (e.g. "New patient forms" / "Insurance & Payment" as seen on the reference site) — kept consistent with this homepage's existing no-stub-links convention.

**Homepage flow below the office scroll reworked — PR open, not yet merged, revised over two rounds of feedback:** Akash reviewed smilemakersfortworth.com's homepage flow and asked for a set of homepage changes below the Trust block's office-photo carousel, keeping everything through that carousel unchanged.

**Current order:** TrustBlock (unchanged through the carousel; Dr. Archana's bio card now also carries her training/affiliation badges; closes with a brief "about our office" line) → TestimonialsSection (right after the office blurb) → InsuranceBlock (redesigned, kept in its existing earlier slot) → ServicesSection (4 items) → LocationMapSection → NewPatientOffersBlock (moved to last on purpose) → BookingBlock → Footer.

- **Meet the team** removed from the homepage entirely (Akash's call) — `team` stays in `content.ts` for a future dedicated `/about` page, not deleted.
- **Insurance** split off from the old combined `InsuranceOfferBlock` into its own `InsuranceBlock` — redesigned from plain check+text pills into typographic "wordmark badge" cards. Real trademarked carrier logo files aren't available/licensed for use here, so this is a deliberate styled-lettering design choice standing in for real logos, not scraped images.
- **New-patient offers** split into its own `NewPatientOffersBlock`, at the very end of the flow per Akash's explicit "keep it last" call.
- **Testimonials (`TestimonialsSection`)** — moved to sit directly after the office blurb per round-2 feedback ("right after 'our office'"). Rebuilt as a continuous auto-scroll loop (same doubled-track/`requestAnimationFrame`/modulo-wrap technique as `OfficeCarousel.tsx`, without drag — nothing to interact with here), with every link removed, including the earlier "Read our reviews on Google →" line (the Google rating/count stays as plain informational text, not a link). Cards hang from a shared connecting rail via a stem + node dot, all moving together as part of the same scrolling track. Quote text is a structural placeholder — real testimonials can't be invented (HIPAA / no-unverifiable-claims, Section 7) — pending real reviews from Akash.
- **Dr. Archana's credentials** moved from a standalone section into her existing bio card in `TrustBlock` ("bring archana's training affiliations along with her bio space" — Akash) — the old `CredentialsSection.tsx` was deleted. Real org names landed 2026-09-02 (PR #60, see the status entry near the top of this section) — no longer behind `<Placeholder>`.
- **Services (`ServicesSection`)** — rebuilt per round-2 feedback to match smilemakersfortworth.com's pattern: trimmed from 6 to exactly 4 items (`content.ts`), single-column stack (not a grid) with a big image tile above compact title/description text, no links of any kind, and a vertical connecting thread + node dot running through the 4 stacked cards, same visual language as the testimonials rail.
  - **Round 3:** real marketing photography Akash supplied (same-day onlay, dental implant, in-office Zoom whitening before/afters — saved to `public/services/`) now backs 3 of the 4 cards, sized with the same `aspect-[4/5]` large-image treatment PR #13 introduced on Dr. Archana's bio card ("bring this as element across our items"). Matched by content: same-day onlay → same-day crowns, implant x-ray → restorative care, whitening → cosmetic dentistry. No photo was supplied for general/preventive care, so that card keeps an icon-tile fallback at the same aspect ratio rather than inventing one. Two other supplied images (Invisalign Gold Provider badge, Masseter Botox before/after) don't map to any of the 4 categories and aren't used yet — flagged in `content.ts` as a follow-up (Botox could pair with Dr. Archana's existing "Certified Botox Provider" credential line; Invisalign could pair with the existing $500-off offer).
- **Map** — unchanged from round 1: real, already-confirmed practice address (standard Google Maps iframe, no API key needed); the "areas we serve" list only confirms Queen Anne as real, the rest are proximity-based placeholders pending Akash's confirmation.
- **Two other open PRs touch overlapping files and are stale relative to current `main`:** `feature/services-page` (PR #5) and `feature/insurance-offer-jtbd-faq` (PR #6) both forked before the office-carousel/ViewportHero/icons work merged, and would delete real content (office photos, `OfficeCarousel.tsx`, `ViewportHero.tsx`) if merged as-is. Flagging here rather than touching them — worth a rebase-or-close decision before they're reviewed.

Deployed on Vercel, connected to `github.com/akashsst-code/supertooth-dentistry`. Production: https://supertooth-dentistry.vercel.app (default Vercel subdomain — no custom domain pointed yet).

**Dr. Archana bio content — PR open, not yet merged:** the Trust block's Dr. Archana card had been carrying a placeholder ("Credentials, years of experience, background story") since Hero v2 landed her headshot. Replaced with her real bio (`content.ts`'s new `archana` export), sourced from the practice's existing site (gray-rail-265889.hostingersite.com and its `/about-us/` page, per Akash): passion for dentistry discovered in 2007, practicing since 2012, DDS from the University of Colorado, Master's in Prosthodontics from India, specializing in implants/crowns/veneers/smile design/implant-supported dentures, philosophy pull-quote, and a certifications line (Invisalign, Botox, research recognition). That site has no personal-life/hobby content beyond her professional passion and philosophy, so none was invented — would violate the no-unverifiable-claims rule (`supertooth-build-principles.md` Section 8).

First pass condensed this down to one trimmed sentence behind a small 96px circular thumbnail; second pass (per Akash: "people trust images more," bring more trust into the card) reversed that — photo now runs large (a real candid shot from an ADA event, not a posed studio headshot: `public/team/archana-profile.jpg`, distinct from the studio headshot `archana.webp` still used in the Hero video panel and team grid), full-bleed edge-to-edge on mobile at roughly half the section's height, side-by-side at ~40%/60% split from `sm:` up. Bio expanded back out to the fuller version above. The two generic badges ("Accepting new patients", "In-network with most insurance") were swapped for her actual credential badges (15+ years experience, University of Colorado, Invisalign certified); the "in-network" claim already lives in the insurance/offer section right below, so keeping it here was redundant.

**Real office photography + trust-block reorder — PR open, not yet merged:** first real office photos landed (`public/office/`, 5 images supplied by Akash), replacing the placeholder office-photo tiles — the first Section 8 content blocker fully resolved for office (team photography is still pending). Because this content is now real, Trust block order changed from the doc's originally-proposed Archana → team → office → reviews to **Archana → office → reviews → team** (Akash's explicit call): office moves up since it's real now, team stays deferred lower since it's still placeholder-only.

Motion (Akash's spec): a genuinely continuous "reel," not a jump-to-next-tile tick — position is driven frame-by-frame via `requestAnimationFrame` and a single CSS transform, not periodic `scrollIntoView` calls (an earlier pass used that; it reads as jerky, not running). The photo list renders twice back-to-back and the offset wraps by one copy's width once it passes that point, so the loop is seamless. A continuously-animating transform can't share an element with native browser scrolling (the two fight over position), so dragging is hand-rolled via pointer events instead of `overflow-x-auto`: pointerdown freezes the reel and starts tracking, pointermove adds the drag delta on top of wherever the reel had gotten to, pointerup either resumes the idle countdown (a drag) or opens the lightbox (a tap — movement stayed under a small px threshold). `touch-action: pan-y` keeps vertical page scroll working through the carousel; only horizontal motion is claimed for the drag.

Tapping/clicking a tile opens it full-size in a lightbox (close button, Escape key, backdrop click, prev/next, focus moved to the close button on open) — serves the anxiety-reduction job office photos exist for (`supertooth-ux-flow.md` Section 2) by letting patients actually look closely at the space. Deliberately no pinch/scroll-to-zoom inside the lightbox — considered and dropped as unneeded complexity; full-size is the actual job to be done here.

Autoplay behavior (Akash's spec): keep a slow, quiet scroll running by default for passive engagement, stop the instant the user touches it, and bring it back on its own once they're done — *unless* they explicitly paused it, which should stick. Modeled as two independent flags rather than one boolean: `userPaused` (sticky, only the pause button sets/clears it) and `interacting` (transient — any pointerdown/wheel/tap sets it and restarts a 4s idle timer that clears it). Autoplay only runs when neither is set (and the lightbox isn't open), so an explicit pause always wins regardless of idle time, and an unpaused carousel always comes back after a few quiet seconds. The pause/play button reflects `userPaused` specifically (the user's stated intent), not the momentary animation state, so its icon doesn't flicker during normal interaction. Autoplay still fully skips itself for `prefers-reduced-motion`, matching `ClinicVideo.tsx` — WCAG 2.2.2 / Section 7 checklist.

**Bug caught during this pass, worth recording:** the rAF loop computed `dt` as raw wall-clock time since the previous frame with no cap. That's fine at a steady 60fps, but the moment a real gap occurs between two frames — the browser tab gets backgrounded and `requestAnimationFrame` gets suspended, then resumes later — the very next callback receives a timestamp reflecting the *entire* gap, and multiplying that by the px/second speed jumps the reel forward by however long it was away (reproduced directly in this session's automation: a background tab suspended rAF almost entirely, and letting it resume produced a ~1300px jump). Fixed by clamping `dt` to a small max (0.1s) per frame — a resumed animation just continues smoothly instead of catching up. This would have hit real users too, any time someone switched tabs/apps mid-visit and came back, so it's a correctness fix, not just a workaround for this session's testing conditions.

Verification note: this session's browser-automation click/scroll/wait tools were unreliable on this hidden/backgrounded tab (Chromium suspends `scrollIntoView({behavior:"smooth"})` and pointer input on hidden tabs — confirmed via `document.visibilityState === "hidden"` — while plain JS timers keep firing). Instant scrollIntoView, the click→lightbox→pixel-level render path, and Escape-to-close were all confirmed working via direct DOM/JS inspection in an earlier pass. This session's idle/resume/sticky-pause state machine was verified the same way: temporarily logged the autoplay effect's inputs, then drove it with dispatched `pointerdown`/`click` events and real wall-clock waits, confirming (a) zero ticks fire while `interacting` or `userPaused`, (b) it auto-resumes ~4s after activity stops, and (c) it stays paused indefinitely through that same idle window when `userPaused` is true. Debug logging was removed before commit. Worth a manual finger-swipe test on a real phone before merge, since that's the one input this session's tooling couldn't simulate directly.

**Trust signals (reviews + insurance) — merged to `main` 2026-08-23 via [PR #4](https://github.com/akashsst-code/supertooth-dentistry/pull/4), reviewed and approved by Akash:** Google reviews now shown as a proper card/strip (Google's G mark, 5-star row, bold rating, review count) instead of a bare number, in both TrustBlock and a compact version in the Hero. Insurance carriers upgraded from generic "Carrier 1-4" pills to named majors (Delta Dental, Premera Blue Cross, Cigna, Aetna — named by Akash as examples of majors to feature, still unconfirmed/`<Placeholder>` pending real network verification) with a "We're in-network with" section, plus the same short list surfaced in the Hero. Hero layout also restructured so Nav+Hero self-fill exactly one mobile screen height (flex-based, no per-device size tuning) after several rounds of real-device feedback exposed that fixed vh/svh budgets don't reliably sum to the actual viewport.

**Logo — merged to `main` 2026-08-23 via [PR #2](https://github.com/akashsst-code/supertooth-dentistry/pull/2), reviewed and approved by Akash:** converged direction from a six-concept board (calligraphic line art, script monogram, gem-cut facets, a cape-and-arms mascot, an academy seal, and this one) — a plain tooth silhouette (`Logo.tsx`, no badge/frame/face) paired with "Supertooth" in heavy italic Fraunces and "Dentistry" in plain tracked caps beneath. No location line in the header (still available in the mobile hamburger menu). Supersedes the two-line name+location lockup mentioned in the Hero v2 entry below. `practice.name` ("Super Tooth Dentistry") is unchanged everywhere else — this stylized wordmark is specific to the nav logo, with the canonical name carried in `aria-label` for screen readers.

**Hero v2 — merged to `main` 2026-08-22, reviewed and approved by Akash:** redesigned around a real trust visual, adapted from smilemakersfortworth.com's video-hero pattern — video-first on mobile, text-left/visual-right split on desktop, eyebrow badges and body copy de-emphasized (lower contrast, smaller) so the visual and the two CTAs (Book your visit / Call) carry the weight instead of competing with them. No office video exists yet, so the interim visual is Dr. Archana's real headshot with a cinematic treatment (slow Ken Burns zoom, gradient vignette, lower-third name caption, small play badge signaling real video is still coming — see `ClinicVideo.tsx`). Swap-in point for real footage is a single component. Nav logo updated to a two-line lockup (name + neighborhood/city), same placement pattern as the reference site.

**Homepage v1 — merged to `main` 2026-08-22, reviewed and approved by Akash:**
- [x] Hero — eyebrow badges, headline, body, Book your visit + Call CTAs
- [x] Trust block — differentiators, Dr. Archana bio card (real photo + trust badges), team grid, office photo grid, Google reviews badge
- [x] Insurance/offer block — carrier chips (placeholder), $149 new-patient offer card, Invisalign $500-off line
- [ ] Booking block — **blocked** on Tab32 service-layer sub-decisions (Section 6)
- [ ] Secondary urgent-path widget ("need to be seen today?")
- [x] Footer — hours/address/parking (placeholder), phone, Book CTA

**Real content landed:** Dr. Archana's headshot (`public/team/archana.webp`) replaces her bio-card and team-grid placeholders — first real content item off the blocker list in Section 8. Everything else in that section (rest of team/office photography, practice details, Tab32 sub-questions) is still open.

**Mobile fix:** the team and office-photo grids used `sm:grid-cols-3` with no mobile column count set, so both collapsed to a single column below 640px — roughly 4 screens of near-empty scroll before the booking CTA on an actual phone. Fixed to `grid-cols-2 sm:grid-cols-3`.

**Remaining before this is launch-ready:** urgent-path widget, booking block (needs Tab32 decisions resolved), CMS/content-editability decision (see platform-pivot doc), replacing remaining placeholders (rest of team/office photos, carrier names, address, hours) with real content — credentials copy landed 2026-09-02, see the status entry near the top of this section — custom domain, and the compliance/accessibility checklist pass (Section 7).

**Hero CTA fix — merged to `main` 2026-08-23, reviewed and approved by Akash ([PR #1](https://github.com/akashsst-code/supertooth-dentistry/pull/1)):** the hero's Call button was landing below the fold on mobile (only Book your visit was visible without scrolling) and its `href` pointed at `/contact` instead of actually dialing. Fixed by putting Book and Call side by side at every width — Book stays the wide primary pill, Call is now a compact icon + "Call" pill (matching the nav's mobile call button) — and wiring Call to a real `tel:` link. Verified at iPhone SE (375px), the md: layout-split boundary (768px, where an interim version briefly broke), iPhone 17 Pro Max (~430px), and 1280px desktop. No other hero spacing/sizing changed.

---

## Status: Webflow build (historical — superseded by the Next.js build above)

Site: "Akash's Fantastic Site" (Webflow site ID `690d3dd0ad47f133f942dcb4`)

**Publish status (checked 2026-08-15):** The homepage sections below were built and published directly on the site's `main` — no Webflow branch was created for this work, so it skipped the branch → staging review → merge sequence this doc's workflow calls for. Akash has reviewed and accepted this as-is (no rollback), so treat it as approved. **All work from this point forward must follow the branch-first workflow** (`docs/supertooth-development-workflow.md`): create a Webflow branch before building a section, publish that branch to staging for review, merge to main only after approval. Site is not yet attached to a custom domain — currently live only on the default Webflow subdomain.

**Pages created:** Home (`/`), Services (`/services`), About & Meet the Team (`/about`), Insurance & New Patients (`/insurance-new-patients`), Contact (`/contact`) — all with SEO title/description set.

**Homepage sections built (in locked order):**
- [x] Hero — eyebrow, insurance signal, headline, body, Book Now + Call CTAs
- [x] Trust block — differentiators, Dr. Archana card, team grid (3 placeholder slots), office photo grid (3 placeholder tiles), Google reviews badge
- [x] Insurance/offer block — carrier chips (placeholder), $149 new-patient offer card, Invisalign $500-off line
- [ ] Booking block — **blocked** on Tab32 service-layer sub-decisions (Section 6)
- [ ] Secondary urgent-path widget ("need to be seen today?")
- [x] Footer — hours/address/parking (placeholder), phone, Book CTA

**Known open item:** Inter and Fraunces are not yet installed on the site — every section currently renders with a fallback font. One-time manual fix: Site Settings → Fonts → add "Inter" and "Fraunces" from Google Fonts in the Webflow Designer. All built CSS already references these font names, so they'll apply automatically once installed.

**Remaining before this is launch-ready:** urgent-path widget, booking block (needs Tab32 decisions resolved), replacing every placeholder (photos, carrier names, address, hours) with real content, and the compliance/accessibility checklist pass (Section 7).
